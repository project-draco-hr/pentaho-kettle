{
  for (Iterator iter=dbDatabaseMeta.iterator(); iter.hasNext(); ) {
    DatabaseMeta dbMeta=(DatabaseMeta)iter.next();
    if (isPoolRegiested(dbMeta))     continue;
    createPool(dbMeta,initialSize,maxiumumSize);
  }
}
