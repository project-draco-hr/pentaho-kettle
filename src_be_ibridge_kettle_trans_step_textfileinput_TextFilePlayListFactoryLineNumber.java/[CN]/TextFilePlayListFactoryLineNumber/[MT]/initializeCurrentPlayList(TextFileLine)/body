{
  File lineFile=AbstractTextFileErrorHandler.getLineNumberFilename(directory,textFileLine.file.getName(),replayDate,extension);
  if (lineFile.exists())   currentPlayList=new TextFilePlayLineNumber(lineFile,encoding,textFileLine.file);
 else   currentPlayList=new TextFilePlayList(textFileLine.file);
}
