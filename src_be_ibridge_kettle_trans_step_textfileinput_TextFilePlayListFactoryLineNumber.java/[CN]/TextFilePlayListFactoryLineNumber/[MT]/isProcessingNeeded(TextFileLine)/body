{
  if (currentPlayList == null || !currentPlayList.getProcessingFile().equals(textFileLine.file))   initializeCurrentPlayList(textFileLine);
  return currentPlayList.isProcessingNeeded(textFileLine.lineNumber);
}
