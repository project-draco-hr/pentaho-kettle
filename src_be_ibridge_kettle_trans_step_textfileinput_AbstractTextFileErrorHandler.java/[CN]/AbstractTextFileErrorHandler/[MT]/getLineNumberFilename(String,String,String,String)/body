{
  String name=null;
  if (extension == null || extension.length() == 0)   name=processingFilename + "." + dateString;
 else   name=processingFilename + "." + dateString+ "."+ extension;
  return new File(Const.replEnv(destinationDirectory),name);
}
