{
  if (Const.isEmpty(wStepname.getText()))   return;
  try {
    getInfo(input);
  }
 catch (  KettleException e) {
    new ErrorDialog(shell,"Erreur","Erreur lors du chargement des informations de l'?tape",e);
  }
  dispose();
}
