{
  String xml=execService(GetJobStatusServlet.CONTEXT_PATH + "/?name=" + URLEncoder.encode(jobName,"UTF-8")+ "&id="+ carteObjectId+ "&xml=Y&from="+ startLogLineNr);
  return SlaveServerJobStatus.fromXML(xml);
}
