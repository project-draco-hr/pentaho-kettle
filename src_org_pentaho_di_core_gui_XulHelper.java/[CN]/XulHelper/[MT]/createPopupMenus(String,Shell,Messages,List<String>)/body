{
  try {
    URL xulFile=getAndValidate(menusFile);
    Document doc=XMLHandler.loadXMLFile(xulFile);
    return MenuHelper.createPopupMenusFromXul(doc,shell,xulMessages,ids);
  }
 catch (  IOException e) {
    throw new KettleException(e);
  }
}
