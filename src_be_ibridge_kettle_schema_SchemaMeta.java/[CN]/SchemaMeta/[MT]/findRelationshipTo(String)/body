{
  int i;
  for (i=0; i < nrRelationships(); i++) {
    RelationshipMeta hi=getRelationship(i);
    if (hi.getTableTo().getName().equalsIgnoreCase(name)) {
      return hi;
    }
  }
  return null;
}
