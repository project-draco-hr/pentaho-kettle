{
  RelationshipMeta[] rels=findRelationshipsUsing(one);
  for (int i=0; i < rels.length; i++) {
    if (!path.contains(rels[i])) {
      path.addRelationship(rels[i]);
      TableMeta next=rels[i].getTableTo();
      if (next.equals(one))       next=rels[i].getTableFrom();
      if (next.equals(two)) {
      }
    }
  }
}
