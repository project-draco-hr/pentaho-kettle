{
  int i;
  for (i=0; i < nrRelationships(); i++) {
    RelationshipMeta ri=getRelationship(i);
    if (ri.getTableFrom().getName().equalsIgnoreCase(name)) {
      return ri;
    }
  }
  return null;
}
