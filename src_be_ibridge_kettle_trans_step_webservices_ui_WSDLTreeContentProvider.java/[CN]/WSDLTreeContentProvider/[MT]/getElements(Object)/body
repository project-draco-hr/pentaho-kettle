{
  Object[] vRet=null;
  if (arg0 instanceof Collection) {
    List vList=new ArrayList((Collection)arg0);
    Collections.sort(vList,new Comparator(){
      public int compare(      Object o1,      Object o2){
        return ((WSDLService)o1).getName().compareTo(((WSDLService)o2).getName());
      }
    }
);
    vRet=vList.toArray();
  }
 else {
    getChildren(arg0);
  }
  return vRet;
}
