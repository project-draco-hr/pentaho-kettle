{
  Row row=getRow();
  if (row == null) {
    setOutputDone();
    return false;
  }
  putRow(row);
  if ((linesRead > 0) && (linesRead % Const.ROWS_UPDATE) == 0)   logBasic(Messages.getString("Injector.Log.LineNumber") + linesRead);
  return true;
}
