{
  if (Const.isEmpty(schemaName))   return tableName;
  if (!isUsingDoubleDecimalAsSchemaTableSeparator())   return schemaName + "." + tableName;
  return databaseInterface.getSchemaTableCombination(schemaName,tableName);
}
