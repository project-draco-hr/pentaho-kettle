{
  if (jge != null) {
    for (    JobHopMeta hi : jobhops) {
      if (hi != null && (hi.getFromEntry() != null) && hi.getFromEntry().equals(jge)) {
        return hi;
      }
    }
  }
  return null;
}
