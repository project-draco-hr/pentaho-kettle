{
  String password=wProxyPassword.getText();
  List<String> list=new ArrayList<String>();
  StringUtil.getUsedVariables(password,list,true);
  if (list.size() == 0) {
    wProxyPassword.setEchoChar('*');
  }
 else {
    wProxyPassword.setEchoChar('\0');
  }
}
