{
  List extensibilityElements=extensibleElement.getExtensibilityElements();
  if (extensibilityElements != null) {
    for (Iterator itr=extensibilityElements.iterator(); itr.hasNext(); ) {
      ExtensibilityElement element=(ExtensibilityElement)itr.next();
      if (element.getElementType().getLocalPart().equalsIgnoreCase(elementType)) {
        return element;
      }
    }
  }
  return null;
}
