{
  if (thread_from == null || thread_from.getPriority() == prior || !thread_from.isAlive())   return true;
  try {
    thread_from.setPriority(prior);
  }
 catch (  Exception e) {
    return false;
  }
  return true;
}
