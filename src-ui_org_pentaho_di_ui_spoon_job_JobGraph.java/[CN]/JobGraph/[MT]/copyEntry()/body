{
  JobEntryCopy[] entries=jobMeta.getSelectedEntries();
  for (int i=0; i < entries.length; i++) {
    if (!canDup(entries[i]))     entries[i]=null;
  }
  spoon.delegates.jobs.copyJobEntries(jobMeta,entries);
}
