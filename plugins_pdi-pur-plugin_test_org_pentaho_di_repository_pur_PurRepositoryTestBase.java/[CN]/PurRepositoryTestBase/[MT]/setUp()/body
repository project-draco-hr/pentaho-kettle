{
  KettleEnvironment.init();
  PentahoSessionHolder.setStrategyName(MODE_GLOBAL);
  mockVersionManager();
  removePentahoRootFolder();
  startMicroPlatform();
  createSystemUser();
  createTester();
  createPurRepository();
  loginAsTester();
}
