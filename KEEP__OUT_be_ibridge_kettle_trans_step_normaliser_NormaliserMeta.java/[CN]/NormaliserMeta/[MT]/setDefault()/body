{
  typeField="typefield";
  int nrfields=0;
  allocate(nrfields);
  for (int i=0; i < nrfields; i++) {
    fieldName[i]="field" + i;
    fieldValue[i]="value" + i;
    fieldNorm[i]="value" + i;
  }
}
