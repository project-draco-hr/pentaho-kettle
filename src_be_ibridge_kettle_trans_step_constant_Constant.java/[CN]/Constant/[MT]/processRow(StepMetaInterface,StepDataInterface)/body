{
  Row r=getRow();
  if (r == null) {
    setOutputDone();
    return false;
  }
  r.addRow(new Row(data.constants));
  putRow(r);
  if (checkFeedback(linesWritten))   logBasic("Linenr " + linesWritten);
  return true;
}
