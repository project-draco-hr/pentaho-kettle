{
  if (parentMenu != null) {
    if (parentMenu.handleMenuEvent(id)) {
      return true;
    }
  }
  if (getMenuBar() != null) {
    if (getMenuBar().handleMenuEvent(id)) {
      return true;
    }
  }
  return false;
}
