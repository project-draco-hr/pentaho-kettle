{
  int index=0;
  for (  RepositoryMeta meta : availableRepositories) {
    if (meta.getName().equals(repositoryMeta.getName())) {
      break;
    }
 else {
      index++;
    }
  }
  return index;
}
