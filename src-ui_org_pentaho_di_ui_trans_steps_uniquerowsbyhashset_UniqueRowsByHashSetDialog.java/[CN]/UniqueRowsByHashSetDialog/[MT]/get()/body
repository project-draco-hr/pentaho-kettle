{
  try {
    RowMetaInterface r=transMeta.getPrevStepFields(stepname);
    if (r != null && !r.isEmpty()) {
      BaseStepDialog.getFieldsFromPrevious(r,wFields,1,new int[]{1},new int[]{},-1,-1,null);
    }
  }
 catch (  KettleException ke) {
    new ErrorDialog(shell,Messages.getString("UniqueRowsByHashSetDialog.FailedToGetFields.DialogTitle"),Messages.getString("UniqueRowsByHashSetDialog.FailedToGetFields.DialogMessage"),ke);
  }
}
