{
  Exception exception=null;
  while (condition.get()) {
    try {
      tracker.findJobTracker(copy);
    }
 catch (    Exception e) {
      condition.set(false);
      exception=e;
      break;
    }
  }
  return exception;
}
