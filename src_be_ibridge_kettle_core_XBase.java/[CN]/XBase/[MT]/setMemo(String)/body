{
  try {
    if (reader.hasMemo()) {
      RandomAccessFile raf=new RandomAccessFile(memo_file,"r");
      reader.setMemoFile(raf);
    }
    return true;
  }
 catch (  Exception e) {
    return false;
  }
}
