{
  String join="";
  if (isComplex()) {
    join=complex_join;
  }
 else   if (table_from != null && table_to != null && fieldnr_from >= 0 && fieldnr_to >= 0) {
    TableField frf=getFieldFrom();
    TableField tof=getFieldTo();
    join=frf.getAliasField() + " = " + tof.getAliasField();
  }
  return join;
}
