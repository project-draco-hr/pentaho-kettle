{
  super(parent,SWT.NONE);
  this.props=props;
  this.rep=rep;
  this.trans=trans;
  this.job=job;
  this.schema=schema;
  shell_text="Select repository object";
  line_text=(trans ? "Select the transformation:" : (job ? "Select the job:" : (schema ? "Select the schema:" : "Select the object")));
  object_name=null;
  object_directory=null;
}
