{
  super(parent,SWT.NONE);
  this.props=props;
  this.rep=rep;
  this.trans=trans;
  this.job=job;
  this.schema=schema;
  shellText="Select repository object";
  lineText=(trans ? "Select the transformation:" : (job ? "Select the job:" : (schema ? "Select the schema:" : "Select the object")));
  objectName=null;
  objectDirectory=null;
}
