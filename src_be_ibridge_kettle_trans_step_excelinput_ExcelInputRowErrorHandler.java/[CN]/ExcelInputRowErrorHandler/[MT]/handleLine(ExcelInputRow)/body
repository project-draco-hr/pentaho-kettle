{
  try {
    getWriter(excelInputRow.sheetName).write(toString(excelInputRow.cells));
    getWriter(excelInputRow.sheetName).write(Const.CR);
  }
 catch (  Exception e) {
    throw new KettleException("Could not create row error for: " + excelInputRow,e);
  }
}
