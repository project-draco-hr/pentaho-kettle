{
  String name=transHopMeta.toString();
  if (log.isDebug())   log.logDebug(toString(),BaseMessages.getString(PKG,"TransGraph.Logging.EditingHop") + name);
  spoon.editHop(transMeta,transHopMeta);
}
