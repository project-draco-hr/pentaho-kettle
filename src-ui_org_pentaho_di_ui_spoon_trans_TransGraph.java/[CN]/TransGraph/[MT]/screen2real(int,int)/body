{
  offset=getOffset();
  Point real;
  if (offset != null) {
    real=new Point(Math.round((x - offset.x) / magnification),Math.round((y - offset.y) / magnification));
  }
 else {
    real=new Point(x,y);
  }
  return real;
}
