{
  if (tips == null) {
    int tipsOfDayCount=toInt(BaseMessages.getString(PKG,"Const.TipOfDay.Count"),0);
    tips=new String[tipsOfDayCount];
    for (int i=1; i <= tipsOfDayCount; i++)     tips[i - 1]=BaseMessages.getString(PKG,"Const.TipOfDay" + Integer.toString(i));
  }
  return tips;
}
