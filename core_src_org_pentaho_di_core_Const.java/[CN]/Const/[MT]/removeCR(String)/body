{
  char[] inArray=in.toCharArray();
  StringBuilder out=new StringBuilder(inArray.length);
  for (  char c : inArray) {
    if (c != '\n') {
      out.append(c);
    }
  }
  return out.toString();
}
