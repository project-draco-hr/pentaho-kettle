{
  script="";
  jsScripts=new ScriptValuesScript[1];
  jsScripts[0]=new ScriptValuesScript(ScriptValuesScript.TRANSFORM_SCRIPT,"Script 1","//Script here");
  int nrfields=0;
  allocate(nrfields);
  for (int i=0; i < nrfields; i++) {
    name[i]="newvalue";
    rename[i]="newvalue";
    type[i]=Value.VALUE_TYPE_NUMBER;
    length[i]=-1;
    precision[i]=-1;
  }
}
