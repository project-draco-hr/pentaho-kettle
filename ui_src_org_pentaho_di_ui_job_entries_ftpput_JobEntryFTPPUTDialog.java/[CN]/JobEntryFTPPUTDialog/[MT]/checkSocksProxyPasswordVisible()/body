{
  String password=wSocksProxyPassword.getText();
  List<String> list=new ArrayList<String>();
  StringUtil.getUsedVariables(password,list,true);
  if (list.size() == 0) {
    wSocksProxyPassword.setEchoChar('*');
  }
 else {
    wSocksProxyPassword.setEchoChar('\0');
  }
}
