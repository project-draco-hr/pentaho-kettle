{
  if (containsComparable(files))   Collections.sort(files);
  if (containsComparable(nonAccessibleFiles))   Collections.sort(nonAccessibleFiles);
  if (containsComparable(nonExistantFiles))   Collections.sort(nonExistantFiles);
}
