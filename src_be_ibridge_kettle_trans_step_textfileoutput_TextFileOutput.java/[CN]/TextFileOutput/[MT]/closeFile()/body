{
  boolean retval=false;
  try {
    if (meta.isZipped()) {
      data.zip.closeEntry();
      data.zip.finish();
      data.zip.close();
    }
 else {
      data.writer.close();
    }
    retval=true;
  }
 catch (  Exception e) {
  }
  return retval;
}
