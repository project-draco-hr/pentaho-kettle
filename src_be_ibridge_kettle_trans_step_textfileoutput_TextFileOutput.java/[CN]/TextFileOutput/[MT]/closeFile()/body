{
  boolean retval=false;
  try {
    data.writer.close();
    if (meta.isZipped()) {
      data.zip.closeEntry();
      data.zip.finish();
      data.zip.close();
    }
    retval=true;
  }
 catch (  Exception e) {
  }
  return retval;
}
