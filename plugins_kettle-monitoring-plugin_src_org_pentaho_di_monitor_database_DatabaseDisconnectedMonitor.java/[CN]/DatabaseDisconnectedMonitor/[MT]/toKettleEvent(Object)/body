{
  if (o == null || !(o instanceof Database)) {
    return null;
  }
  DatabaseEvent event=new DatabaseEvent(DatabaseEvent.EventType.DISCONNECTED).build(((Database)o).getDatabaseMeta());
  logInfo("[PDI Extension Point Plugin] Dispathing to Event Bus " + event.toString());
  return event;
}
