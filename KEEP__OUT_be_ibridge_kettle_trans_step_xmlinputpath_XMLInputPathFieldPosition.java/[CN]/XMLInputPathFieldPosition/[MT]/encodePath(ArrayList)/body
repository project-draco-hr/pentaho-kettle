{
  String encoded="";
  for (int p=0; p < path.size(); p++) {
    XMLInputPathFieldPosition pos=(XMLInputPathFieldPosition)path.get(p);
    String elementName=pos.toString();
    if (p > 0)     encoded+=XMLInputPathField.POSITION_MARKER;
    encoded+=elementName;
  }
  return encoded;
}
