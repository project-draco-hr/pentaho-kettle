{
  boolean connexion=false;
  if (wProtocol.getText().equals("FTP"))   connexion=connectToFTP();
 else   if (wProtocol.getText().equals("SFTP"))   connexion=connectToSFTP();
 else   if (wProtocol.getText().equals("SSH"))   connexion=connectToSSH();
  return connexion;
}
