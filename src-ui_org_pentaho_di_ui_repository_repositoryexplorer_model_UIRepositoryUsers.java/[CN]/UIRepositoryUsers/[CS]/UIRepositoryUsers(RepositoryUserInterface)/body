{
  String[] logins;
  try {
    logins=rui.getUserLogins();
    for (    String login : logins) {
      this.add(new UIRepositoryUser(rui.loadUserInfo(login)));
    }
  }
 catch (  Exception e) {
  }
}
