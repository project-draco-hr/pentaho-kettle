{
  String[] logins;
  try {
    logins=rsm.getUserLogins();
    for (    String login : logins) {
      this.add(new UIRepositoryUser(rsm.loadUserInfo(login)));
    }
  }
 catch (  Exception e) {
  }
}
