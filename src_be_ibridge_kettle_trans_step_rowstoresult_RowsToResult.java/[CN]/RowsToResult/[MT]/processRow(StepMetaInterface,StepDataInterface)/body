{
  meta=(RowsToResultMeta)smi;
  data=(RowsToResultData)sdi;
  Row r=getRow();
  if (r == null) {
    getTransMeta().getResultRows().addAll(data.rows);
    setOutputDone();
    return false;
  }
  data.rows.add(r);
  putRow(r);
  if ((linesRead > 0) && (linesRead % Const.ROWS_UPDATE) == 0)   logBasic(Messages.getString("RowsToResult.Log.LineNumber") + linesRead);
  return true;
}
