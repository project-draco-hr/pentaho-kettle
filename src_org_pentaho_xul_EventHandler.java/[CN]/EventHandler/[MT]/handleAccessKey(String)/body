{
  try {
    XulMenuChoice menuItem=(XulMenuChoice)keyMap.get(accessKey);
    if (menuItem != null) {
      handleMenuEvent(menuItem.getId());
    }
  }
 catch (  Throwable t) {
    t.printStackTrace();
  }
}
