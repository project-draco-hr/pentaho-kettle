{
  String name=null;
  String sourceAdding="";
  if (source != DUMMY_SOURCE) {
    sourceAdding="_" + source.toString();
  }
  if (extension == null || extension.length() == 0)   name=processingFilename + sourceAdding + "."+ dateString;
 else   name=processingFilename + sourceAdding + "."+ dateString+ "."+ extension;
  return new File(Const.replEnv(destinationDirectory),name);
}
