{
  boolean saved=false;
  log.logBasic(toString(),Messages.getString("Spoon.Log.SaveAs"));
  if (rep != null) {
    meta.setID(-1L);
    saved=saveToRepository(meta,true);
    renameTabs();
  }
 else {
    saved=saveXMLFile(meta);
    renameTabs();
  }
  refreshTree();
  return saved;
}
