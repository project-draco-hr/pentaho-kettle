{
  StringBuffer line=new StringBuffer(250);
  int c=reader.read();
  while (c == '\n' || c == '\r') {
    c=reader.read();
  }
  while (c != '\n' && c != '\r') {
    line.append((char)c);
    c=reader.read();
  }
  return line.toString();
}
