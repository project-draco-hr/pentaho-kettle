{
  if (open) {
    try {
      close();
    }
 catch (    IOException e) {
      log.debug("Error on close in BlobBuffer.UnicodeInputStream.finalize()");
    }
 finally {
      super.finalize();
    }
  }
}
