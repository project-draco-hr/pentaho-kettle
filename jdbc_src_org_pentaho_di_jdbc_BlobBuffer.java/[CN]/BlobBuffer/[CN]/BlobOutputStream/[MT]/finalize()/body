{
  if (open) {
    try {
      close();
    }
 catch (    IOException e) {
      log.debug("Error on close in Buffer.BlobOutputStream.finalize()");
    }
 finally {
      super.finalize();
    }
  }
}
