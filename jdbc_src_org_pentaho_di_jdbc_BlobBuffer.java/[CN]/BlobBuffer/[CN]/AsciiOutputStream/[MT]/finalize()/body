{
  if (open) {
    try {
      close();
    }
 catch (    IOException e) {
      log.debug("Error on close in Buffer.AsciiOutputStream.finalize()");
    }
 finally {
      super.finalize();
    }
  }
}
