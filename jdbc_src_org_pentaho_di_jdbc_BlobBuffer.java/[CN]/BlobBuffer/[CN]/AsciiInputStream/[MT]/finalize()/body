{
  if (open) {
    try {
      close();
    }
 catch (    IOException e) {
      log.debug("Error on close in Buffer.AsciiInputStream.finalize()");
    }
 finally {
      super.finalize();
    }
  }
}
