{
  try {
    if (raFile != null) {
      raFile.close();
    }
  }
 catch (  IOException e) {
    log.debug("Error on close in BlobBuffer.finalize()");
  }
 finally {
    if (blobFile != null) {
      blobFile.delete();
    }
  }
}
