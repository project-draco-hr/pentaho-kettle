{
  repository.connectionDelegate.performDelete("DELETE FROM " + quoteTable(KettleDatabaseRepository.TABLE_R_NAMESPACE) + " WHERE "+ quote(KettleDatabaseRepository.FIELD_NAMESPACE_ID_NAMESPACE)+ " = ? ",namespaceId);
}
