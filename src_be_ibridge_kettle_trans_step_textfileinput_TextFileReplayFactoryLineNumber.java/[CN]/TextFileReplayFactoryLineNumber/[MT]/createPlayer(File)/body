{
  File lineFile=AbstractTextFileLineErrorHandler.getLineNumberFilename(directory,file.getName(),replayDate,extension);
  if (!lineFile.exists())   return TextFilePlayerNone.INSTANCE;
  return new TextFilePlayLineNumber(lineFile,encoding);
}
