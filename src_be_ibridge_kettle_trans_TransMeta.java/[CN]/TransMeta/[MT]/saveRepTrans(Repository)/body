{
  rep.insertTransformation(getID(),getName(),readStep == null ? -1 : readStep.getID(),writeStep == null ? -1 : writeStep.getID(),inputStep == null ? -1 : inputStep.getID(),outputStep == null ? -1 : outputStep.getID(),updateStep == null ? -1 : updateStep.getID(),logConnection == null ? -1 : logConnection.getID(),logTable,useBatchId,logfieldUsed,maxDateConnection == null ? -1 : maxDateConnection.getID(),maxDateTable,maxDateField,maxDateOffset,maxDateDifference,modifiedUser,modifiedDate,sizeRowset,directory.getID());
}
