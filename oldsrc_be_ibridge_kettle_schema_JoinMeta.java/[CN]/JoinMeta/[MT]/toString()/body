{
  if (fieldnr_from >= 0 && fieldnr_to >= 0) {
    return table_from.getName() + "." + table_from.getField(fieldnr_from).getName()+ " - "+ table_to.getName()+ "."+ table_to.getField(fieldnr_to).getName();
  }
 else {
    return table_from.getName() + " - " + table_to.getName();
  }
}
