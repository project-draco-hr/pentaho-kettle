{
  Row out=getFields(null,stepinfo.getName(),info);
  if (out != null) {
    for (int i=0; i < out.size(); i++) {
      Value outvalue=out.getValue(i);
      DatabaseImpact di=new DatabaseImpact(DatabaseImpact.TYPE_IMPACT_READ,transMeta.getName(),stepinfo.getName(),databaseMeta.getDatabaseName(),"",outvalue.getName(),outvalue.getName(),stepinfo.getName(),sql,Messages.getString("DatabaseJoinMeta.DatabaseImpact.Title"));
      impact.add(di);
    }
  }
}
