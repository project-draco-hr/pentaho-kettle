{
  if (!Const.isEmpty(partitionID)) {
    return stepname + "." + partitionID;
  }
 else   if (clusterSize > 1) {
    return stepname + "." + slaveNr+ "."+ getCopy();
  }
 else {
    return stepname + "." + getCopy();
  }
}
