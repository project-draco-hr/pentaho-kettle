{
  if (isWaitingForData())   return false;
  for (  RowSet rowSet : inputRowSets) {
    if (rowSet.size() > 0) {
      return false;
    }
  }
  for (  RowSet rowSet : inputRowSets) {
    if (!isPassingData() || !rowSet.isBlocking()) {
      return false;
    }
  }
  return true;
}
