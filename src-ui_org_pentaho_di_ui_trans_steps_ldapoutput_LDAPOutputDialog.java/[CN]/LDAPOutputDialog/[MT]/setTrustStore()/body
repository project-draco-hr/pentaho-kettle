{
  boolean enable=wsetTrustStore.getSelection() && LDAPConnection.getProtocolFromCode(wProtocol.getText()) != LDAPConnection.PROTOCOL_LDAP;
  wlTrustAll.setEnabled(enable);
  wTrustAll.setEnabled(enable);
  trustAll();
}
