{
  int max=-99999;
  for (int i=0; i < schema.nrTables(); i++) {
    TableMeta si=schema.getTable(i);
    if (si.isSelected()) {
      Point p=si.getLocation();
      if (p.y > max)       max=p.y;
    }
  }
  for (int i=0; i < schema.nrTables(); i++) {
    TableMeta si=schema.getTable(i);
    if (si.isSelected()) {
      Point p=si.getLocation();
      p.y=max;
    }
  }
  redraw();
}
