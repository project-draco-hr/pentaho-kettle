{
  StringBuffer rest=new StringBuffer(wLogging.getText());
  rest.append(XMLHandler.date2string(new Date())).append(" : ");
  rest.append(line).append(Const.CR);
  wLogging.setText(rest.toString());
  wLogging.setSelection(rest.length());
}
