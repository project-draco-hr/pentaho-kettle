{
  if (repositoryElement.getObjectId() == null) {
    try {
      if (getJobID(repositoryElement.getName(),repositoryElement.getRepositoryDirectory().getObjectId()) != null)       return true;
    }
 catch (    KettleException dbe) {
      return true;
    }
  }
  return false;
}
