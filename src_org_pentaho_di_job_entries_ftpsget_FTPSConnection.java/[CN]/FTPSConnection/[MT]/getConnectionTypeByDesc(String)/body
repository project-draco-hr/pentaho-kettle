{
  if (tt == null)   return 0;
  for (int i=0; i < connection_type_Desc.length; i++) {
    if (connection_type_Desc[i].equalsIgnoreCase(tt))     return i;
  }
  return getConnectionTypeByCode(tt);
}
