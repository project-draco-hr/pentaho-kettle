{
  endBuffer++;
  if (endBuffer >= bufferSize) {
    resizeByteBufferArray();
    if (!readBufferFromFile()) {
      if (endBuffer >= bufferSize) {
        return true;
      }
    }
  }
  return false;
}
