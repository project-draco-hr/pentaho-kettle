{
  bb.position(endBuffer);
  int n=fc.read(bb);
  if (n == -1) {
    return false;
  }
 else {
    bufferSize=endBuffer + n;
    bb.position(endBuffer);
    bb.get(byteBuffer,endBuffer,n);
    return true;
  }
}
