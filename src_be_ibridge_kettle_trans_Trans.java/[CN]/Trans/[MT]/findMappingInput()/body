{
  for (int i=0; i < steps.size(); i++) {
    StepMetaDataCombi smdc=(StepMetaDataCombi)steps.get(i);
    StepInterface step=smdc.step;
    if (step.getStepID().equalsIgnoreCase("MappingInput"))     return (MappingInput)step;
  }
  return null;
}
