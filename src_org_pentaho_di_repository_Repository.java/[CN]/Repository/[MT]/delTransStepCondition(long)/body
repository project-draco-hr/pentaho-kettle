{
  String sql="DELETE FROM " + quoteTable(Repository.TABLE_R_TRANS_STEP_CONDITION) + " WHERE "+ quote(Repository.FIELD_TRANS_STEP_CONDITION_ID_TRANSFORMATION)+ " = "+ id_transformation;
  execStatement(sql);
}
