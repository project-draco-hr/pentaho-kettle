{
  return connectionDelegate.getStrings("SELECT " + quote(Repository.FIELD_DIRECTORY_DIRECTORY_NAME) + " FROM "+ quoteTable(Repository.TABLE_R_DIRECTORY)+ " WHERE "+ quote(Repository.FIELD_DIRECTORY_ID_DIRECTORY_PARENT)+ " = "+ id_directory+ " ORDER BY "+ quote(Repository.FIELD_DIRECTORY_DIRECTORY_NAME));
}
