{
  return connectionDelegate.getIDs("SELECT " + quote(Repository.FIELD_CONDITION_ID_CONDITION) + " FROM "+ quoteTable(Repository.TABLE_R_CONDITION)+ " WHERE "+ quote(Repository.FIELD_CONDITION_ID_CONDITION_PARENT)+ " = "+ id_condition);
}
