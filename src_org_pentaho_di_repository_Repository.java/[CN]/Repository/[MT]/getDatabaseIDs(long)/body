{
  return connectionDelegate.getIDs("SELECT " + quote(Repository.FIELD_STEP_DATABASE_ID_DATABASE) + " FROM "+ quoteTable(Repository.TABLE_R_STEP_DATABASE)+ " WHERE "+ quote(Repository.FIELD_STEP_DATABASE_ID_TRANSFORMATION)+ " = "+ id_transformation);
}
