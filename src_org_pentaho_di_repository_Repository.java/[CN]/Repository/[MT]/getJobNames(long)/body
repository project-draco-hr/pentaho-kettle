{
  return connectionDelegate.getStrings("SELECT " + quote(Repository.FIELD_JOB_NAME) + " FROM "+ quoteTable(Repository.TABLE_R_JOB)+ " WHERE "+ quote(Repository.FIELD_JOB_ID_DIRECTORY)+ " = "+ id_directory+ " ORDER BY "+ quote(Repository.FIELD_JOB_NAME));
}
