{
  return connectionDelegate.getIDs("SELECT " + quote(Repository.FIELD_PARTITION_ID_PARTITION) + " FROM "+ quoteTable(Repository.TABLE_R_PARTITION)+ " WHERE "+ quote(Repository.FIELD_PARTITION_ID_PARTITION_SCHEMA)+ " = "+ id_partition_schema);
}
