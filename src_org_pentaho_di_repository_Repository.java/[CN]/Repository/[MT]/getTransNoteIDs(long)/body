{
  return connectionDelegate.getIDs("SELECT " + quote(Repository.FIELD_TRANS_NOTE_ID_NOTE) + " FROM "+ quoteTable(Repository.TABLE_R_TRANS_NOTE)+ " WHERE "+ quote(Repository.FIELD_TRANS_NOTE_ID_TRANSFORMATION)+ " = "+ id_transformation);
}
