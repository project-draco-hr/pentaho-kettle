{
  String nameField=quote(Repository.FIELD_DATABASE_NAME);
  return connectionDelegate.getStrings("SELECT " + nameField + " FROM "+ quoteTable(Repository.TABLE_R_DATABASE)+ " ORDER BY "+ nameField);
}
