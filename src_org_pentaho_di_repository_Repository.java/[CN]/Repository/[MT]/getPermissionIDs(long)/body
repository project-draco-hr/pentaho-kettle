{
  return connectionDelegate.getIDs("SELECT " + quote(Repository.FIELD_PROFILE_PERMISSION_ID_PERMISSION) + " FROM "+ quoteTable(Repository.TABLE_R_PROFILE_PERMISSION)+ " WHERE "+ quote(Repository.FIELD_PROFILE_PERMISSION_ID_PROFILE)+ " = "+ id_profile);
}
