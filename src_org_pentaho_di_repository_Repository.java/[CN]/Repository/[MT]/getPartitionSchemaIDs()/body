{
  return connectionDelegate.getIDs("SELECT " + quote(Repository.FIELD_PARTITION_SCHEMA_ID_PARTITION_SCHEMA) + " FROM "+ quoteTable(Repository.TABLE_R_PARTITION_SCHEMA)+ " ORDER BY "+ quote(Repository.FIELD_PARTITION_SCHEMA_NAME));
}
