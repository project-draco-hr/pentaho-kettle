{
  return connectionDelegate.getIDs("SELECT " + quote(Repository.FIELD_STEP_ID_STEP) + " FROM "+ quoteTable(Repository.TABLE_R_STEP)+ " WHERE "+ quote(Repository.FIELD_STEP_ID_TRANSFORMATION)+ " = "+ id_transformation);
}
