{
  return connectionDelegate.getIDs("SELECT " + quote(Repository.FIELD_DATABASE_ID_DATABASE) + " FROM "+ quoteTable(Repository.TABLE_R_DATABASE)+ " ORDER BY "+ quote(Repository.FIELD_DATABASE_ID_DATABASE));
}
