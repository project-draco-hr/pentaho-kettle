{
  String sql="SELECT DISTINCT " + quote(Repository.FIELD_TRANS_PARTITION_SCHEMA_ID_TRANSFORMATION) + " FROM "+ quoteTable(Repository.TABLE_R_TRANS_PARTITION_SCHEMA)+ " WHERE "+ quote(Repository.FIELD_TRANS_PARTITION_SCHEMA_ID_PARTITION_SCHEMA)+ " = "+ id_partition_schema;
  return transDelegate.getTransformationsWithIDList(connectionDelegate.getRows(sql,100),connectionDelegate.getReturnRowMeta());
}
