{
  return connectionDelegate.getIDs("SELECT " + quote(Repository.FIELD_JOB_NOTE_ID_NOTE) + " FROM "+ quoteTable(Repository.TABLE_R_JOB_NOTE)+ " WHERE "+ quote(Repository.FIELD_JOB_NOTE_ID_JOB)+ " = "+ id_job);
}
