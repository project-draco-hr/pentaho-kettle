{
  return connectionDelegate.getStrings("SELECT " + quote(Repository.FIELD_TRANSFORMATION_NAME) + " FROM "+ quoteTable(Repository.TABLE_R_TRANSFORMATION)+ " WHERE "+ quote(Repository.FIELD_TRANSFORMATION_ID_DIRECTORY)+ " = "+ id_directory+ " ORDER BY "+ quote(Repository.FIELD_TRANSFORMATION_NAME));
}
