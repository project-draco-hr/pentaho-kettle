{
  return connectionDelegate.getIDs("SELECT " + quote(Repository.FIELD_JOB_HOP_ID_JOB_HOP) + " FROM "+ quoteTable(Repository.TABLE_R_JOB_HOP)+ " WHERE "+ quote(Repository.FIELD_JOB_HOP_ID_JOB)+ " = "+ id_job);
}
