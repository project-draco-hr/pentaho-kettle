{
  String nameField=quote(Repository.FIELD_PARTITION_SCHEMA_NAME);
  return connectionDelegate.getStrings("SELECT " + nameField + " FROM "+ quoteTable(Repository.TABLE_R_PARTITION_SCHEMA)+ " ORDER BY "+ nameField);
}
