{
  String nameField=quote(Repository.FIELD_PROFILE_NAME);
  return connectionDelegate.getStrings("SELECT " + nameField + " FROM "+ quoteTable(Repository.TABLE_R_PROFILE)+ " ORDER BY "+ nameField);
}
