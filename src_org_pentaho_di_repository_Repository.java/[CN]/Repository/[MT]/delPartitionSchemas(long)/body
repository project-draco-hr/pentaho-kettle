{
  String sql="DELETE FROM " + quoteTable(Repository.TABLE_R_TRANS_PARTITION_SCHEMA) + " WHERE "+ quote(Repository.FIELD_TRANS_PARTITION_SCHEMA_ID_TRANSFORMATION)+ " = "+ id_transformation;
  execStatement(sql);
}
