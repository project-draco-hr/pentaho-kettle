{
  return connectionDelegate.getIDs("SELECT " + quote(Repository.FIELD_CLUSTER_SLAVE_ID_SLAVE) + " FROM "+ quoteTable(Repository.TABLE_R_CLUSTER_SLAVE)+ " WHERE "+ quote(Repository.FIELD_CLUSTER_SLAVE_ID_CLUSTER)+ " = "+ id_cluster_schema);
}
