{
  return connectionDelegate.getIDs("SELECT " + quote(Repository.FIELD_DEPENDENCY_ID_DEPENDENCY) + " FROM "+ quoteTable(Repository.TABLE_R_DEPENDENCY)+ " WHERE "+ quote(Repository.FIELD_DEPENDENCY_ID_TRANSFORMATION)+ " = "+ id_transformation);
}
