{
  String nameField=quote(Repository.FIELD_CLUSTER_NAME);
  return connectionDelegate.getStrings("SELECT " + nameField + " FROM "+ quoteTable(Repository.TABLE_R_CLUSTER)+ " ORDER BY "+ nameField);
}
