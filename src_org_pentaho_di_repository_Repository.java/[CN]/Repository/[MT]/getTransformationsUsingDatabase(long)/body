{
  String sql="SELECT DISTINCT " + quote(Repository.FIELD_STEP_DATABASE_ID_TRANSFORMATION) + " FROM "+ quoteTable(Repository.TABLE_R_STEP_DATABASE)+ " WHERE "+ quote(Repository.FIELD_STEP_DATABASE_ID_DATABASE)+ " = "+ id_database;
  return transDelegate.getTransformationsWithIDList(connectionDelegate.getRows(sql,100),connectionDelegate.getReturnRowMeta());
}
