{
  return connectionDelegate.getIDs("SELECT " + quote(Repository.FIELD_JOBENTRY_ID_JOBENTRY) + " FROM "+ quoteTable(Repository.TABLE_R_JOBENTRY)+ " WHERE "+ quote(Repository.FIELD_JOBENTRY_ID_JOB)+ " = "+ id_job);
}
