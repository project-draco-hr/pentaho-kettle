{
  return connectionDelegate.getIDs("SELECT " + quote(Repository.FIELD_DATABASE_ATTRIBUTE_ID_DATABASE_ATTRIBUTE) + " FROM "+ quoteTable(Repository.TABLE_R_DATABASE_ATTRIBUTE)+ " WHERE "+ quote(Repository.FIELD_DATABASE_ATTRIBUTE_ID_DATABASE)+ " = "+ id_database);
}
