{
  String sql="DELETE FROM " + quoteTable(Repository.TABLE_R_TRANS_ATTRIBUTE) + " WHERE "+ quote(Repository.FIELD_TRANS_ATTRIBUTE_ID_TRANSFORMATION)+ " = "+ id_transformation;
  execStatement(sql);
}
