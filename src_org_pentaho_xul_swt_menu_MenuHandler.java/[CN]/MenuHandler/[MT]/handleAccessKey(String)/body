{
  try {
    MenuChoice menuItem=(MenuChoice)keyMap.get(accessKey);
    if (menuItem != null) {
      handleMenuEvent(menuItem.getId());
    }
  }
 catch (  Throwable t) {
    t.printStackTrace();
  }
}
