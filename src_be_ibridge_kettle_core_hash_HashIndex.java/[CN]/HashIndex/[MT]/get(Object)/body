{
  int hash=key.hashCode();
  hash^=(hash >>> 20) ^ (hash >>> 12);
  hash=hash ^ (hash >>> 7) ^ (hash >>> 4);
  for (Entry e=table[(hash & (table.length - 1))]; e != null; e=e.next) {
    E k;
    if (e.hash == hash && key.equals(k=e.entry))     return k;
  }
  return null;
}
