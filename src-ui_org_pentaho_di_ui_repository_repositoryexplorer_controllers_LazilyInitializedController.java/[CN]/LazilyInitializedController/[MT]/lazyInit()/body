{
  if (!initialized) {
    boolean succeeded=false;
    try {
      succeeded=doLazyInit();
    }
 catch (    Exception e) {
      e.printStackTrace();
      showErrorDialog(e);
    }
    if (succeeded) {
      initialized=true;
    }
 else {
      showErrorDialog(null);
    }
  }
}
