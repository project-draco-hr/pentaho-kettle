{
  for (int i=0; i < outputRowsField.length; i++) {
    ValueMetaInterface value=new ValueMeta(outputRowsField[i],outputRowsType[i],outputRowsLength[i],outputRowsPrecision[i]);
    row.addValueMeta(value);
  }
}
