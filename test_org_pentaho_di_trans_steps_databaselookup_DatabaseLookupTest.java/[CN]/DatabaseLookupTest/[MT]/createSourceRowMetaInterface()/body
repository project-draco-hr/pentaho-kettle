{
  RowMetaInterface rm=new RowMeta();
  ValueMetaInterface[] valuesMeta={new ValueMetaInteger("ID",8,0),new ValueMetaInteger("CODE",8,0),new ValueMetaString("STRING",30,0)};
  for (int i=0; i < valuesMeta.length; i++) {
    rm.addValueMeta(valuesMeta[i]);
  }
  return rm;
}
