{
  KettleVariables vars=local.createKettleVariables(Thread.currentThread().getName(),null,false);
  vars.setVariable(TODAY,"main thread initial value");
  TLTest test=new TLTest(Thread.currentThread());
  test.start();
}
