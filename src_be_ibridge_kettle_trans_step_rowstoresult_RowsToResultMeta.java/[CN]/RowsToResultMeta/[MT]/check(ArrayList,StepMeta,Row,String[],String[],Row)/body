{
  if (input.length > 0) {
    CheckResult cr=new CheckResult(CheckResult.TYPE_RESULT_OK,Messages.getString("RowsToResultMeta.CheckResult.StepReceivingInfoFromOtherSteps"),stepinfo);
    remarks.add(cr);
  }
 else {
    CheckResult cr=new CheckResult(CheckResult.TYPE_RESULT_ERROR,Messages.getString("RowsToResultMeta.CheckResult.NoInputReceivedError"),stepinfo);
    remarks.add(cr);
  }
}
