{
  report.append(Messages.getString("DatabaseDialog.report.Hostname")).append(hostName).append(Const.CR);
  report.append(Messages.getString("DatabaseDialog.report.Port")).append(portNumber).append(Const.CR);
  report.append(Messages.getString("DatabaseDialog.report.DatabaseName")).append(dbName).append(Const.CR);
  return report;
}
