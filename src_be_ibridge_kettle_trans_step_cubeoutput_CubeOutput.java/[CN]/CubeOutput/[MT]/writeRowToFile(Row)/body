{
  try {
    if (first) {
      r.write(data.dos);
      first=false;
    }
 else {
      r.writeData(data.dos);
    }
  }
 catch (  Exception e) {
    logError(Messages.getString("CubeOutput.Log.ErrorWritingLine") + e.toString());
    return false;
  }
  linesOutput++;
  return true;
}
