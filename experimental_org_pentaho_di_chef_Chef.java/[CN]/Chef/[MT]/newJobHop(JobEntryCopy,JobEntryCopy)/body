{
  log.logBasic(toString(),Messages.getString("Chef.Log.NewJobHop") + fr.getName() + ", "+ to.getName()+ ")");
  JobHopMeta hi=new JobHopMeta(fr,to);
  jobMeta.addJobHop(hi);
  addUndoNew(new JobHopMeta[]{hi},new int[]{jobMeta.indexOfJobHop(hi)});
  refreshGraph();
  refreshTree();
}
