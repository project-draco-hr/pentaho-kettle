{
  try {
    Long d=rowMeta.getInteger(currentRow,index - 1);
    if (d == null) {
      lastNull=true;
      return 0;
    }
    lastNull=false;
    return d;
  }
 catch (  Exception e) {
    throw new SQLException(e);
  }
}
