{
  RowMetaInterface rm=new RowMeta();
  ValueMetaInterface valuesMeta[]={new ValueMeta("PARAM1",ValueMeta.TYPE_STRING),new ValueMeta("PARAM2",ValueMeta.TYPE_STRING)};
  for (int i=0; i < valuesMeta.length; i++) {
    rm.addValueMeta(valuesMeta[i]);
  }
  return rm;
}
