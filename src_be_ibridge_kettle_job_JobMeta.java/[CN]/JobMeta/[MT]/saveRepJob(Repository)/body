{
  try {
    rep.insertJob(getID(),directory.getID(),getName(),logconnection == null ? -1 : logconnection.getID(),logtable,modified_user,modified_date);
  }
 catch (  KettleDatabaseException dbe) {
    throw new KettleException("Unable to save job info to repository",dbe);
  }
}
