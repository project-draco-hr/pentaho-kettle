{
  try {
    logBasic(Messages.getString("Normaliser.Log.StartingToRun"));
    while (processRow(meta,data) && !isStopped())     ;
  }
 catch (  Throwable t) {
    logError(Messages.getString("Normaliser.Log.UnexpectedError") + " : " + t.toString());
    logError(Const.getStackTracker(t));
    setErrors(1);
    stopAll();
  }
 finally {
    dispose(meta,data);
    logSummary();
    markStop();
  }
}
