{
  if (o == null || !(o instanceof JobExecutionExtension)) {
    return null;
  }
  JobEntryEvent event=new JobEntryEvent(EventType.Job.AFTER_JOB_ENTRY).build((JobExecutionExtension)o);
  return event;
}
