{
  String enc="";
  for (int i=0; i < xmlInputFieldPositions.length; i++) {
    XMLInputFieldPosition pos=xmlInputFieldPositions[i];
    if (i > 0)     enc+=", ";
    enc+=pos.toString();
  }
  return enc;
}
