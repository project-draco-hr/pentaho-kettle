{
  String s=properties.getProperty(IGNORE_LISTENERS);
  if (s != null) {
    StringTokenizer st=new StringTokenizer(s,",");
    while (st.hasMoreTokens()) {
      String list=st.nextToken();
      if (list.equalsIgnoreCase(name))       return true;
    }
    return false;
  }
  return false;
}
