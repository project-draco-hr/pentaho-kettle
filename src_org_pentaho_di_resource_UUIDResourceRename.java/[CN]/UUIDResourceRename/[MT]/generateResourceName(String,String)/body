{
  String uuid=UUIDUtil.getUUIDAsString();
  return (prefix != null ? prefix : "") + uuid + ((extension != null && extension.length() > 0) ? (extension.charAt(0) == '.' ? extension : "." + extension) : "");
}
