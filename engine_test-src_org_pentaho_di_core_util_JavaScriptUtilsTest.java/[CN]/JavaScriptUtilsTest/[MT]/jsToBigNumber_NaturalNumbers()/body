{
  Number[] naturalNumbers=new Number[]{(byte)1,(short)1,1,(long)1};
  for (  Number number : naturalNumbers) {
    assertEquals(1.0,jsToBigNumber(number,number.getClass().getName()).doubleValue(),1e-6);
  }
}
