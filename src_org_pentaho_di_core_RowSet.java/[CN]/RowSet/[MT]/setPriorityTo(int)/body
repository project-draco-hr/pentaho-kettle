{
  if (thread_to == null || thread_to.getPriority() == prior || !thread_to.isAlive())   return true;
  try {
    thread_to.setPriority(prior);
  }
 catch (  Exception e) {
    return false;
  }
  return true;
}
