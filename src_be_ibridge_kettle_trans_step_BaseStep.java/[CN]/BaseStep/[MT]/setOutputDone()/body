{
  if (log.isDebug())   logDebug(Messages.getString("BaseStep.Log.OutputDone",String.valueOf(outputRowSets.size())));
  for (int i=0; i < outputRowSets.size(); i++) {
    RowSet rs=(RowSet)outputRowSets.get(i);
    rs.setDone();
  }
}
