{
  appendMessage(report,"DatabaseDialog.report.Hostname",hostName);
  appendMessage(report,"DatabaseDialog.report.Port",portNumber);
  appendMessage(report,"DatabaseDialog.report.DatabaseName",dbName);
  return report;
}
