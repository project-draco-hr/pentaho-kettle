{
  if (isStopped())   return;
  try {
    data.helper.importData(meta.getModelName(),fieldList,data.batchRows);
    for (int i=0; i < data.batchRows.size(); i++)     incrementLinesOutput();
  }
  finally {
    data.batchRows.clear();
  }
}
