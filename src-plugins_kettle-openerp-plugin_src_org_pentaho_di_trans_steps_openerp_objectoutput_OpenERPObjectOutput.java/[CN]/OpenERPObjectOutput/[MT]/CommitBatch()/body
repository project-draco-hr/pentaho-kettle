{
  if (isStopped() || data.batchRows.size() == 0)   return;
  try {
    openerERPAdapter.importData(data.batchRows);
    for (int i=0; i < data.batchRows.size(); i++)     incrementLinesOutput();
  }
  finally {
    data.batchRows.clear();
  }
}
