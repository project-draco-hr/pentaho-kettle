{
  GetJobSQLProgressDialog pspd=new GetJobSQLProgressDialog(shell,jobMeta,rep);
  ArrayList stats=pspd.open();
  if (stats != null) {
    if (stats.size() > 0) {
      SQLStatementsDialog ssd=new SQLStatementsDialog(shell,SWT.NONE,stats);
      ssd.open();
    }
 else {
      MessageBox mb=new MessageBox(shell,SWT.OK | SWT.ICON_INFORMATION);
      mb.setMessage(Messages.getString("Message.MessageBox.NoSQLNeedEexecuted"));
      mb.setText(Messages.getString("Message.MessageBox.SQL"));
      mb.open();
    }
  }
}
