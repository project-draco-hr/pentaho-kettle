{
  String name=getName();
  if (name.equalsIgnoreCase(oldname))   return name;
  int nr=2;
  while (Const.findDatabase(databases,getName()) != null) {
    setName(name + " " + nr);
    nr++;
  }
  return getName();
}
