{
  DatabaseMeta di=new DatabaseMeta(getName(),getDatabaseTypeDesc(),getAccessTypeDesc(),getHostname(),getDatabaseName(),getDatabasePortNumber(),getUsername(),getPassword());
  di.setServername(getServername());
  di.setDataTablespace(getDataTablespace());
  di.setIndexTablespace(getIndexTablespace());
  return di;
}
