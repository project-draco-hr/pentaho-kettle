{
  wFormat.removeAll();
  int valtype=Value.getType(wValueType.getText());
switch (valtype) {
case Value.VALUE_TYPE_NUMBER:
    for (int i=0; i < Const.number_formats.length; i++)     wFormat.add(Const.number_formats[i]);
  break;
case Value.VALUE_TYPE_DATE:
for (int i=0; i < Const.date_formats.length; i++) wFormat.add(Const.date_formats[i]);
break;
case Value.VALUE_TYPE_STRING:
case Value.VALUE_TYPE_BOOLEAN:
case Value.VALUE_TYPE_INTEGER:
default :
break;
}
}
