{
  StringBuffer value;
  if (val != null) {
    value=new StringBuffer(val);
    Const.repl(value,"&","&amp;");
    Const.repl(value,"'","&apos;");
    Const.repl(value,"<","&lt;");
    Const.repl(value,">","&gt;");
    Const.repl(value,"\"","&quot;");
  }
 else {
    value=new StringBuffer();
  }
  return "<" + tag + ">"+ value+ "</"+ tag+ ">"+ (cr ? Const.CR : "");
}
