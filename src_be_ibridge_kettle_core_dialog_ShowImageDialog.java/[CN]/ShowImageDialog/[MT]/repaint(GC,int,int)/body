{
  ImageData imd=image.getImageData();
  if (scaling) {
    wSComp.setMinWidth(width);
    wSComp.setMinHeight(height);
  }
  if (scaling && (prefHeight < 0 || prefWidth < 0)) {
    gc.drawImage(image,0,0,imd.width,imd.height,0,0,width,height);
  }
 else {
    gc.drawImage(image,0,0);
  }
}
