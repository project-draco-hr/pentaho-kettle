{
  meta=(SortedMergeMeta)smi;
  data=(SortedMergeData)sdi;
  Object[] row=getRowSorted();
  if (row == null) {
    setOutputDone();
    return false;
  }
  putRow(data.rowMeta,row);
  if (checkFeedback(getLinesRead()))   logBasic(Messages.getString("SortedMerge.Log.LineNumber") + getLinesRead());
  return true;
}
