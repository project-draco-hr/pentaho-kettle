{
  Exception exception=null;
  while (condition.get()) {
    try {
      PluginRegistry.getInstance().getPlugins(type);
    }
 catch (    Exception e) {
      condition.set(false);
      exception=e;
      break;
    }
  }
  return exception;
}
