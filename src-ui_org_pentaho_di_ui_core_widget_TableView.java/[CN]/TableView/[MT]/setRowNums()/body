{
  for (int i=0; i < table.getItemCount(); i++) {
    TableItem item=table.getItem(i);
    if (item != null) {
      String num="" + (i + 1);
      if (!item.getText(0).equals(num)) {
        item.setText(0,num);
      }
    }
  }
}
