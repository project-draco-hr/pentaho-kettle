{
  try {
    getInfo(input);
  }
 catch (  KettleException e) {
    new ErrorDialog(shell,Messages.getString("SalesforceInputDialog.ErrorValidateData.DialogTitle"),Messages.getString("SalesforceInputDialog.ErrorValidateData.DialogMessage"),e);
  }
  dispose();
}
