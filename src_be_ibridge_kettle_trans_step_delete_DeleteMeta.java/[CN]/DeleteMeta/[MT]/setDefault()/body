{
  keyStream=null;
  database=null;
  commitSize=0;
  tableName=Messages.getString("DeleteMeta.DefaultTableName.Label");
  int nrkeys=0;
  allocate(nrkeys);
  for (int i=0; i < nrkeys; i++) {
    keyLookup[i]="age";
    keyCondition[i]="BETWEEN";
    keyStream[i]="age_from";
    keyStream2[i]="age_to";
  }
}
