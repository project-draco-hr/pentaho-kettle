{
  List list=new ArrayList();
  Row r=new Row();
  Value values[]={new Value("field1","KETTLE1"),new Value("field2",123L),new Value("field3",10.5D),new Value("field4",new Date()),new Value("field5",true),new Value("field6",new BigDecimal(123.45)),new Value("field7",new BigDecimal(123.60))};
  for (int i=0; i < values.length; i++) {
    r.addValue(values[i]);
  }
  Row r1=new Row();
  Value values1[]={new Value("field1","KETTLE1"),new Value("field2",500L),new Value("field3",20.0D),new Value("field4",new Date()),new Value("field5",false),new Value("field6",new BigDecimal(123.45)),new Value("field7",new BigDecimal(123.60))};
  for (int i=0; i < values1.length; i++) {
    r1.addValue(values1[i]);
  }
  Row r2=new Row();
  Value values2[]={new Value("field1","KETTLE1"),new Value("field2",500L),new Value("field3",20.0D),new Value("field4",new Date()),new Value("field5",false),new Value("field6",new BigDecimal(123.45)),new Value("field7",new BigDecimal(123.60))};
  for (int i=0; i < values2.length; i++) {
    r2.addValue(values2[i]);
  }
  list.add(r);
  list.add(r1);
  list.add(r2);
  return list;
}
