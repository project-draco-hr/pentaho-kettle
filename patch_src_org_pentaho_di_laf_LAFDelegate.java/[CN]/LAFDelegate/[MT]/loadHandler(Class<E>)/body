{
  E h=null;
  try {
    if (c.isAssignableFrom(handlerClass.getClass()))     ;
    h=(E)c.newInstance();
  }
 catch (  Exception e) {
    e.printStackTrace();
  }
  if (h != null) {
    changeHandler(h);
  }
  return h;
}
