{
  StringBuffer retval=new StringBuffer(300);
  if (validatorField != null)   for (int i=0; i < validatorField.length; i++) {
    retval.append("       ").append(validatorField[i].getXML()).append(Const.CR);
  }
  return retval.toString();
}
