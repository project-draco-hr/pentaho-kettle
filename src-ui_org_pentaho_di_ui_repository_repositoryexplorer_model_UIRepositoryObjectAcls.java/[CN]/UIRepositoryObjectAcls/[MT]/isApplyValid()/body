{
  if (isModelValid() && isModelDirty()) {
    this.firePropertyChange("applyValid",null,true);
    return true;
  }
 else {
    this.firePropertyChange("applyValid",null,false);
    return false;
  }
}
