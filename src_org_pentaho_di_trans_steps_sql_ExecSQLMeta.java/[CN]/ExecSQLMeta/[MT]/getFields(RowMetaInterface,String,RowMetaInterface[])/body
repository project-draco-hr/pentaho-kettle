{
  Object[] add=ExecSQL.getResultRow(new Result(),getUpdateField(),getInsertField(),getDeleteField(),getReadField());
  for (int i=0; i < add.length; i++)   r.addValueMeta(((ValueMetaAndData)add[i]).getValueMeta());
}
