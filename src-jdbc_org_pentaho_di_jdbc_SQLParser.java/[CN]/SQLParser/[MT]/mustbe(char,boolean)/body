{
  if (in[s] != c) {
    throw new SQLException(BaseMessages.getString(PKG,"error.parsesql.mustbe",String.valueOf(s),String.valueOf(c)),"22019");
  }
  if (copy) {
    out[d++]=in[s++];
  }
 else {
    s++;
  }
}
