{
  try {
    OverlayProperties ph=new OverlayProperties(filename);
    if (ph != null) {
      propList.addFirst(ph);
      return true;
    }
    return false;
  }
 catch (  IOException e) {
    return false;
  }
}
