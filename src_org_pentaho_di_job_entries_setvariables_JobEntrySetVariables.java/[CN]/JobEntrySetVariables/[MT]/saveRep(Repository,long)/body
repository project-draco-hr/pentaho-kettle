{
  try {
    super.saveRep(rep,id_job);
    rep.saveJobEntryAttribute(id_job,getID(),"replacevars",replaceVars);
    if (variableName != null) {
      for (int i=0; i < variableName.length; i++) {
        rep.saveJobEntryAttribute(id_job,getID(),i,"variable_name",variableName[i]);
        rep.saveJobEntryAttribute(id_job,getID(),i,"variable_value",variableValue[i]);
        rep.saveJobEntryAttribute(id_job,getID(),i,"variable_type",getVariableTypeCode(variableType[i]));
      }
    }
  }
 catch (  KettleDatabaseException dbe) {
    throw new KettleException(BaseMessages.getString(PKG,"JobEntrySetVariables.Meta.UnableSaveRep",String.valueOf(id_job),dbe.getMessage()),dbe);
  }
}
