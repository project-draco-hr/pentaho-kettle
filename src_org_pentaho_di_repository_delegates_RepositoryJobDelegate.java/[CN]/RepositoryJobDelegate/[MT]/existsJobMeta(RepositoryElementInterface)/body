{
  if (repositoryElement.getID() < 0) {
    try {
      if (getJobID(repositoryElement.getName(),repositoryElement.getRepositoryDirectory().getID()) > 0)       return true;
    }
 catch (    KettleException dbe) {
      return true;
    }
  }
  return false;
}
