{
  boolean saved=false;
  log.logBasic(toString(),Messages.getString("Spoon.Log.SaveAs"));
  if (rep != null) {
    transMeta.setID(-1L);
    saved=saveRepository(true);
  }
 else {
    saved=saveXMLFile();
  }
  return saved;
}
