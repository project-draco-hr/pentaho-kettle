{
  if (Const.isEmpty(transMeta.getName()) && Const.isEmpty(transMeta.getFilename()))   return STRING_TRANS_NO_NAME;
  if (Const.isEmpty(transMeta.getName()))   transMeta.nameFromFilename();
  return transMeta.getName();
}
