{
  TreeItem ti[]=selectionTree.getSelection();
  StepMeta inf=null;
  if (ti.length == 1) {
    String steptype=ti[0].getText();
    log.logDebug(toString(),Messages.getString("Log.NewStep") + steptype);
    inf=newStep(steptype,steptype,openit,rename);
  }
  return inf;
}
