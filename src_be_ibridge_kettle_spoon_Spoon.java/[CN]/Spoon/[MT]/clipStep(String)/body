{
  log.logDebug(toString(),Messages.getString("Log.CopyStepToClipboard") + name);
  StepMeta stepMeta=transMeta.findStep(name);
  if (stepMeta != null) {
    String xml=stepMeta.getXML();
    toClipboard(xml);
  }
}
