{
  log.logDebug(toString(),Messages.getString("Spoon.Log.CopyStepToClipboard") + name);
  StepMeta stepMeta=transMeta.findStep(name);
  if (stepMeta != null) {
    String xml=stepMeta.getXML();
    toClipboard(xml);
  }
}
