{
  metaStore=mock(IMetaStore.class);
  metaStoreElementType=mock(IMetaStoreElementType.class);
  when(metaStore.getElementTypeByName(anyString(),eq(StarDomainMetaStoreUtil.METASTORE_STAR_DOMAIN_TYPE_NAME))).thenReturn(metaStoreElementType);
}
