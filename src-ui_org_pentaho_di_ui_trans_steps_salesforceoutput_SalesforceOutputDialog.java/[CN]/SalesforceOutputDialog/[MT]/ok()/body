{
  try {
    getInfo(input);
  }
 catch (  KettleException e) {
    new ErrorDialog(shell,BaseMessages.getString(PKG,"SalesforceOutputDialog.ErrorValidateData.DialogTitle"),BaseMessages.getString(PKG,"SalesforceOutputDialog.ErrorValidateData.DialogMessage"),e);
  }
  dispose();
}
