{
  if (tt == null)   return 0;
  for (int i=0; i < removeSpecialCharactersDesc.length; i++) {
    if (removeSpecialCharactersDesc[i].equalsIgnoreCase(tt))     return i;
  }
  return getRemoveSpecialCharactersByCode(tt);
}
