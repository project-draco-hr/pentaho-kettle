{
  String retval;
  if (getTable() != null && getDBName() != null) {
    if (!isExact()) {
      retval=getTable().getName() + "." + getDBName();
    }
 else {
      retval=getDBName();
    }
  }
 else {
    retval="??";
  }
  return retval;
}
