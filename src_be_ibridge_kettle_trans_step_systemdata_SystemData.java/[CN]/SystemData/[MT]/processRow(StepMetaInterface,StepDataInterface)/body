{
  Row row;
  if (data.readsRows) {
    row=getRow();
    if (row == null) {
      setOutputDone();
      return false;
    }
  }
 else {
    row=new Row();
    linesRead++;
  }
  row.addRow(getSystemData());
  if (log.isRowLevel())   logRowlevel("System info returned: " + row);
  putRow(row);
  setOutputDone();
  return true;
}
