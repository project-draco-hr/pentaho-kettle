{
  if (job != null && !job.isActive()) {
    job=null;
    log.logMinimal(Chef.APP_NAME,Messages.getString("ChefLog.Log.JobHasEnded"));
  }
  if (!wStart.isDisposed()) {
    if (job != null)     wStart.setText(STOP_TEXT);
 else     wStart.setText(START_TEXT);
  }
}
