{
  for (int i=0; i < nrJobEntriesWithType(JobPlugin.TYPE_ALL); i++) {
    JobPlugin sp=getJobEntryWithType(JobPlugin.TYPE_ALL,i);
    if (jei.getClass().getName().equals(sp.getClassname()))     return sp.getID();
  }
  return null;
}
