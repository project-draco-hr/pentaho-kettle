{
  for (int i=0; i < pluginList.size(); i++) {
    JobPlugin sp=(JobPlugin)pluginList.get(i);
    if (sp.getDescription().equalsIgnoreCase(description) || sp.getID().equalsIgnoreCase(description))     return sp;
  }
  return null;
}
