{
  meta=(PropertyOutputMeta)smi;
  data=(PropertyOutputData)sdi;
  try {
    if (data.file != null) {
      data.file.close();
      data.file=null;
    }
  }
 catch (  Exception ex) {
  }
  ;
  setOutputDone();
  super.dispose(smi,sdi);
}
