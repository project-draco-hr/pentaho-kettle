{
  meta=(PropertyOutputMeta)smi;
  data=(PropertyOutputData)sdi;
  if (data.file != null) {
    try {
      data.file.close();
      data.file=null;
    }
 catch (    Exception ex) {
    }
    ;
  }
  setOutputDone();
  super.dispose(smi,sdi);
}
