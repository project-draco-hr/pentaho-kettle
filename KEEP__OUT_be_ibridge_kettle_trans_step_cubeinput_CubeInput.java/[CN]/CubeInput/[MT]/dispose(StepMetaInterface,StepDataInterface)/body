{
  meta=(CubeInputMeta)smi;
  data=(CubeInputData)sdi;
  try {
    data.dis.close();
    data.fis.close();
  }
 catch (  IOException e) {
    logError(Messages.getString("CubeInput.Log.ErrorClosingCube") + e.toString());
    setErrors(1);
    stopAll();
  }
  super.dispose(smi,sdi);
}
