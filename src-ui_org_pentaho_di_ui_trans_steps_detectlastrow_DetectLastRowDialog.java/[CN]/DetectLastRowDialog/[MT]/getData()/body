{
  if (log.isDebug())   log.logDebug(toString(),Messages.getString("DetectLastRowDialog.Log.GettingKeyInfo"));
  if (input.getResultFieldName() != null)   wResult.setText(input.getResultFieldName());
  wStepname.selectAll();
}
