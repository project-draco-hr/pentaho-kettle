{
  if (log.isDebug())   logDebug(BaseMessages.getString(PKG,"DetectLastRowDialog.Log.GettingKeyInfo"));
  if (input.getResultFieldName() != null)   wResult.setText(input.getResultFieldName());
  wStepname.selectAll();
}
