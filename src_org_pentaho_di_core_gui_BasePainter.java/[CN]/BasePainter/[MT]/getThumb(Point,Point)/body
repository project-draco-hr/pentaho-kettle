{
  Point resizedMax=magnifyPoint(transMax);
  Point thumb=new Point(0,0);
  if (resizedMax.x <= area.x)   thumb.x=100;
 else   thumb.x=Math.round(100f * area.x / resizedMax.x);
  if (resizedMax.y <= area.y)   thumb.y=100;
 else   thumb.y=Math.round(100f * area.y / resizedMax.y);
  return thumb;
}
