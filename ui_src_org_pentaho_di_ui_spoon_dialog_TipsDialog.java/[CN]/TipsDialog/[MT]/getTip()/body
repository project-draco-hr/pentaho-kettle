{
  String retval="";
  int tipnr=props.getTipNr();
  String[] tips=Const.getTips();
  if (tipnr <= tips.length - 1) {
    retval=tips[tipnr];
  }
  tipnr++;
  if (tipnr > tips.length - 1) {
    tipnr=0;
  }
  props.setTipNr(tipnr);
  return retval;
}
