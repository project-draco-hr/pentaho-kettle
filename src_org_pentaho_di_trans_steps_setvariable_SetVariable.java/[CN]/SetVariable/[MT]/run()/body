{
  try {
    logBasic(Messages.getString("SetVariable.Log.StartingToRun"));
    while (processRow(meta,data) && !isStopped())     ;
  }
 catch (  Throwable t) {
    logError(Messages.getString("SetVariable.RuntimeError.UnexpectedError.SETVARIABLE0003",t.toString()));
    logError(Const.getStackTracker(t));
    setErrors(1);
    stopAll();
  }
 finally {
    dispose(meta,data);
    logSummary();
    markStop();
  }
}
