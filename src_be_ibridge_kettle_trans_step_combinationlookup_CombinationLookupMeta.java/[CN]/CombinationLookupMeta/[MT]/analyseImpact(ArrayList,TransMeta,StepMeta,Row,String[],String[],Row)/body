{
  for (int i=0; i < keyField.length; i++) {
    Value v=prev.searchValue(keyField[i]);
    DatabaseImpact ii=new DatabaseImpact(DatabaseImpact.TYPE_IMPACT_READ_WRITE,transMeta.getName(),stepMeta.getName(),database.getDatabaseName(),tablename,keyLookup[i],keyField[i],v != null ? v.getOrigin() : "?","",useHash ? "read and insert" : "key lookup and insert");
    impact.add(ii);
  }
  if (useHash) {
    DatabaseImpact ii=new DatabaseImpact(DatabaseImpact.TYPE_IMPACT_READ_WRITE,transMeta.getName(),stepMeta.getName(),database.getDatabaseName(),tablename,hashField,"","","","key lookup on hash-field");
    impact.add(ii);
  }
}
