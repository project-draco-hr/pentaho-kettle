{
  tablename=Messages.getString("CombinationLookupMeta.DimensionTableName.Label");
  database=null;
  commitSize=0;
  replaceFields=true;
  useHash=false;
  hashField="hashcode";
  int nrkeys=0;
  allocate(nrkeys);
  for (int i=0; i < nrkeys; i++) {
    keyField[i]="key" + i;
    keyLookup[i]="keylookup" + i;
  }
  technicalKeyField="technical /surrogate key field";
  useAutoinc=false;
}
