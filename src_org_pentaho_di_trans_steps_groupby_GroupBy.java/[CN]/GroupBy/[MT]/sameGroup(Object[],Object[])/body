{
  for (int i=0; i < data.groupnrs.length; i++) {
    Object prev=previous[data.groupnrs[i]];
    Object curr=r[data.groupnrs[i]];
    if (data.previousMeta.getValueMeta(data.groupnrs[i]).compare(prev,curr) != 0)     return false;
  }
  return true;
}
