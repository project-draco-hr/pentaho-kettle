{
  if (!ctx.canIgnoreField(field)) {
    try {
      Object val=field.get(obj);
      if (field.getType().isPrimitive() || ctx.canTreatAsPrimitive(val))       return true;
    }
 catch (    IllegalAccessException e) {
    }
  }
  return false;
}
