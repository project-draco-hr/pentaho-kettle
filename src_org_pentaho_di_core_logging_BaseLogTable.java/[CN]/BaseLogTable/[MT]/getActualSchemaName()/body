{
  if (!Const.isEmpty(schemaName))   return space.environmentSubstitute(schemaName);
  String name=space.getVariable(getSchemaNameVariable());
  if (Const.isEmpty(name)) {
    return null;
  }
 else {
    return name;
  }
}
