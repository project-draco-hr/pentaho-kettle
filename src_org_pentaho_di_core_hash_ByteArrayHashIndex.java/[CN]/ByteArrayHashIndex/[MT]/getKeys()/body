{
  List<byte[]> list=new ArrayList<byte[]>();
  for (int i=0; i < table.length; i++) {
    Entry entry=table[i];
    if (entry != null && entry.key != null) {
      list.add(entry.key);
    }
  }
  return list;
}
