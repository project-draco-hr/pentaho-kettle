{
  ValueMetaInterface valueMeta=new MetaInterfaceBuilder(TYPE_NUMBER).length(17).precision(0).build();
  assertEquals(STRING_BIGINT,dbMeta.getFieldDefinition(valueMeta,null,null,false,false,false));
}
