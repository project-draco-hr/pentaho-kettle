{
  ValueMetaInterface valueMeta=new MetaInterfaceBuilder(ValueMetaInterface.TYPE_NUMBER).length(0).precision(0).build();
  assertEquals(STRING_INT,dbMeta.getFieldDefinition(valueMeta,null,null,false,false,false));
}
