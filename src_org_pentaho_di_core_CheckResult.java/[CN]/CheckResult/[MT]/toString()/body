{
  StringBuffer sb=new StringBuffer();
  sb.append(typeDesc[type]).append(": ").append(text);
  if (stepMeta != null)   sb.append(" (").append(stepMeta.getName()).append(")");
  return sb.toString();
}
