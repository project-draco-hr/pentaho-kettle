{
  debug="start of processRow()";
  Row row=getRowFromXML();
  if (row == null) {
    setOutputDone();
    return false;
  }
  logRowlevel("Read row: " + row.toString());
  putRow(row);
  return true;
}
