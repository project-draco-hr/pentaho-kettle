{
  StringBuffer retval=new StringBuffer();
  if (calculation != null)   for (int i=0; i < calculation.length; i++) {
    retval.append("       " + calculation[i].getXML() + Const.CR);
  }
  return retval.toString();
}
