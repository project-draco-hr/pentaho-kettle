{
  if (readPtr >= length) {
    return -1;
  }
  if (raFile != null) {
    if (currentPage != (readPtr & PAGE_MASK)) {
      readPage(readPtr);
    }
    return buffer[readPtr & BYTE_MASK] & 0xFF;
  }
 else {
    return buffer[readPtr] & 0xFF;
  }
}
