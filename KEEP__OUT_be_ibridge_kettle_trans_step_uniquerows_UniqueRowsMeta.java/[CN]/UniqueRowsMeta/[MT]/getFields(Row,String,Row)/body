{
  Row row;
  if (r == null)   row=new Row();
 else   row=r;
  if (countRows) {
    Value v=new Value(countField,Value.VALUE_TYPE_INTEGER);
    v.setLength(9,0);
    v.setOrigin(name);
    r.addValue(v);
  }
  return row;
}
