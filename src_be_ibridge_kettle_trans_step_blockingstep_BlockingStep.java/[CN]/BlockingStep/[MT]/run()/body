{
  try {
    while (processRow((StepMetaInterface)meta,data) && !isStopped())     ;
  }
 catch (  Exception e) {
    setErrors(1);
    stopAll();
  }
 finally {
    logSummary();
    markStop();
  }
}
