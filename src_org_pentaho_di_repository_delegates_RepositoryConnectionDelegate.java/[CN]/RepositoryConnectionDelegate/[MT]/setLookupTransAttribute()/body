{
  String sql="SELECT " + quote(Repository.FIELD_TRANS_ATTRIBUTE_VALUE_STR) + ", "+ quote(Repository.FIELD_TRANS_ATTRIBUTE_VALUE_NUM)+ " FROM "+ databaseMeta.getQuotedSchemaTableCombination(null,Repository.TABLE_R_TRANS_ATTRIBUTE)+ " WHERE "+ quote(Repository.FIELD_TRANS_ATTRIBUTE_ID_TRANSFORMATION)+ " = ?  AND "+ quote(Repository.FIELD_TRANS_ATTRIBUTE_CODE)+ " = ? AND "+ Repository.FIELD_TRANS_ATTRIBUTE_NR+ " = ? ";
  psTransAttributesLookup=database.prepareSQL(sql);
}
