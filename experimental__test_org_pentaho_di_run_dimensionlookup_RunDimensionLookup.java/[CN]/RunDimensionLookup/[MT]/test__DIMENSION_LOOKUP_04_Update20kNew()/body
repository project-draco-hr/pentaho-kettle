{
  TimedTransRunner timedTransRunner=new TimedTransRunner("experimental_test/org/pentaho/di/run/dimensionlookup/DimensionLookupUpdate20k.ktr",LogWriter.LOG_LEVEL_ERROR,20000);
  timedTransRunner.init();
  timedTransRunner.runNewEngine(true);
  Result newResult=timedTransRunner.getNewResult();
  assertTrue(newResult.getNrErrors() == 0);
}
