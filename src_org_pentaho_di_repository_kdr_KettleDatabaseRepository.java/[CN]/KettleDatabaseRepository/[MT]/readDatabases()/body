{
  List<DatabaseMeta> databases=new ArrayList<DatabaseMeta>();
  ObjectId[] ids=getDatabaseIDs();
  for (int i=0; i < ids.length; i++) {
    DatabaseMeta databaseMeta=loadDatabaseMeta(ids[i]);
    databases.add(databaseMeta);
  }
  return databases;
}
