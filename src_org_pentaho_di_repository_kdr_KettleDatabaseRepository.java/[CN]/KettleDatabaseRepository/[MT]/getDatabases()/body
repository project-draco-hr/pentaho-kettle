{
  List<DatabaseMeta> list=new ArrayList<DatabaseMeta>();
  ObjectId[] databaseIDs=getDatabaseIDs();
  for (int i=0; i < databaseIDs.length; i++) {
    DatabaseMeta databaseMeta=loadDatabaseMeta(databaseIDs[i]);
    list.add(databaseMeta);
  }
  return list;
}
