{
  int i;
  for (i=0; i < nrRelationships(); i++) {
    RelationshipMeta ri=getRelationship(i);
    if (ri.getTableFrom().getName().equalsIgnoreCase(from) && ri.getTableTo().getName().equalsIgnoreCase(to)) {
      return ri;
    }
  }
  return null;
}
