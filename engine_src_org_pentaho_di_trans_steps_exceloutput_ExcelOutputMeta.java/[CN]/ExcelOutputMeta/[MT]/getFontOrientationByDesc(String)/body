{
  if (tt == null)   return 0;
  for (int i=0; i < font_orientation_desc.length; i++) {
    if (font_orientation_desc[i].equalsIgnoreCase(tt))     return i;
  }
  return getFontOrientationByCode(tt);
}
