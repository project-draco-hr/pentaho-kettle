{
  if (repositoryElement.getObjectId() == null) {
    try {
      if (getTransformationID(repositoryElement.getName(),repositoryElement.getRepositoryDirectory().getObjectId()) != null)       return true;
    }
 catch (    KettleException dbe) {
      return true;
    }
  }
  return false;
}
