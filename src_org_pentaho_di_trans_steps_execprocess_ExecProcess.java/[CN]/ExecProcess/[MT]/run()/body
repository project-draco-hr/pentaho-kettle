{
  if (log.isBasic())   logBasic(BaseMessages.getString(PKG,"ExecProcess.Log.StartingToRun"));
  try {
    while (processRow(meta,data) && !isStopped())     ;
  }
 catch (  Exception e) {
    logError(BaseMessages.getString(PKG,"ExecProcess.Log.UnexpectedError") + " : " + e.toString());
    logError(Const.getStackTracker(e));
    setErrors(1);
    stopAll();
  }
 finally {
    dispose(meta,data);
    logSummary();
    markStop();
  }
}
