{
  this.shell=shell;
  this.dbMeta=dbInfo;
  if (schemaName != null) {
    this.tableName=dbInfo.getSchemaTableCombination(schemaName,tableName);
  }
 else {
    this.tableName=tableName;
  }
  this.limit=limit;
}
