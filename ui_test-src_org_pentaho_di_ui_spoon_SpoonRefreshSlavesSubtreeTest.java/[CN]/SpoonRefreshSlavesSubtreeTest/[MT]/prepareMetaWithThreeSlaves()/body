{
  AbstractMeta meta=mock(AbstractMeta.class);
  List<SlaveServer> servers=asList(mockServer("1"),mockServer("2"),mockServer("3"));
  when(meta.getSlaveServers()).thenReturn(servers);
  return meta;
}
