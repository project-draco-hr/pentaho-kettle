{
switch (method) {
case PARTITIONING_METHOD_MOD:
    return getMethodDescription() + " : " + fieldName+ "@"+ partitionSchema.getName();
case PARTITIONING_METHOD_HASH:
  return getMethodDescription() + " : " + fieldName+ "@"+ partitionSchema.getName();
default :
return getMethodDescription();
}
}
