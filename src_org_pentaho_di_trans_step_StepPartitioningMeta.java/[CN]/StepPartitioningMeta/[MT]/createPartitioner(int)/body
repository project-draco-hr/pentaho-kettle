{
switch (method) {
case PARTITIONING_METHOD_MOD:
    partitioner=new ModPartitioner(this);
  break;
case PARTITIONING_METHOD_HASH:
partitioner=new HashPartitioner(this);
break;
case PARTITIONING_METHOD_NONE:
partitioner=new NoPartitioner(this);
break;
default :
partitioner=null;
}
}
