{
  int i, nrfields=0;
  allocate(nrfields);
  for (i=0; i < nrfields; i++) {
    fieldName[i]="field" + i;
    fieldType[i]=ValueMetaInterface.TYPE_STRING;
    fieldLength[i]=30;
    fieldPrecision[i]=-1;
  }
}
