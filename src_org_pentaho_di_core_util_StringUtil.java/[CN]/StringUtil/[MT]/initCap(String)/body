{
  if (st == null || st.trim().length() == 0)   return "";
  StringBuffer sb=new StringBuffer();
  Matcher m=ALL_LETTERS.matcher(st);
  if (m.find(0))   m.appendReplacement(sb,m.group(1).toUpperCase());
  st=m.appendTail(sb).toString();
  return st;
}
