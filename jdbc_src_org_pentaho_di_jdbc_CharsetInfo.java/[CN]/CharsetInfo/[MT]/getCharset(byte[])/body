{
  CharsetInfo charset;
  if (collation[4] != 0) {
    charset=getCharsetForSortOrder(collation[4] & 0xFF);
  }
 else {
    charset=getCharsetForLCID((collation[2] & 0x0F) << 16 | (collation[1] & 0xFF) << 8 | (collation[0] & 0xFF));
  }
  if (charset == null) {
    throw new SQLException(BaseMessages.getString(PKG,"error.charset.nocollation",Support.toHex(collation)),"2C000");
  }
  return charset;
}
