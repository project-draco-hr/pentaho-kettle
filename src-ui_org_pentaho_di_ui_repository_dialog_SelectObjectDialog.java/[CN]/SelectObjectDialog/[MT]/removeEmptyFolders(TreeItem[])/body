{
  for (  TreeItem item : treeitems) {
    if (item.getImage().equals(GUIResource.getInstance().getImageArrow()) && item.getItemCount() == 0)     item.dispose();
 else     removeEmptyFolders(item.getItems());
  }
}
