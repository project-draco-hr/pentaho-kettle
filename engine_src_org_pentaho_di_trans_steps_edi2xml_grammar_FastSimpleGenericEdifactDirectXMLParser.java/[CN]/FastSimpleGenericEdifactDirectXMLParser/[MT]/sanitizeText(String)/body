{
  if (txt.indexOf("?") >= 0) {
    txt=txt.replace("?+","+");
    txt=txt.replace("?:",":");
    txt=txt.replace("?'","'");
    txt=txt.replace("??","?");
  }
  return StringEscapeUtils.escapeXml(txt);
}
