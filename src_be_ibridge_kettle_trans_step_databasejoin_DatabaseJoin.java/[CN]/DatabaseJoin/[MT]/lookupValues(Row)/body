{
  debug=Messages.getString("DatabaseJoin.Log.StartOfLookupValues");
  if (first) {
    first=false;
    debug=Messages.getString("DatabaseJoin.Log.SetLookupStatement");
    logDetailed(Messages.getString("DatabaseJoin.Log.CheckingRow") + row.toString());
    data.keynrs=new int[meta.getParameterField().length];
    debug=Messages.getString("DatabaseJoin.Log.GetKeyfiedNumbers");
    for (int i=0; i < meta.getParameterField().length; i++) {
      data.keynrs[i]=row.searchValueIndex(meta.getParameterField()[i]);
      if (data.keynrs[i] < 0) {
        throw new KettleStepException(Messages.getString("DatabaseJoin.Exception.FieldNotFound",meta.getParameterField()[i]));
      }
    }
  }
  debug=Messages.getString("DatabaseJoin.Log.GetParameters");
  Row param=new Row();
  for (int i=0; i < data.keynrs.length; i++) {
    param.addValue(row.getValue(data.keynrs[i]));
  }
  debug=Messages.getString("DatabaseJoin.Log.OpenQuery");
  ResultSet rs=data.db.openQuery(data.pstmt,param);
  debug=Messages.getString("DatabaseJoin.Log.GetRow");
  Row add=data.db.getRow(rs);
  linesInput++;
  int counter=0;
  while (add != null && (meta.getRowLimit() == 0 || counter < meta.getRowLimit())) {
    counter++;
    Row newrow=new Row(row);
    newrow.addRow(add);
    putRow(newrow);
    logRowlevel(Messages.getString("DatabaseJoin.Log.PutoutRow") + add);
    if (meta.getRowLimit() == 0 || counter < meta.getRowLimit()) {
      add=data.db.getRow(rs);
      linesInput++;
    }
  }
  if (counter == 0 && meta.isOuterJoin()) {
    if (data.notfound == null) {
      data.notfound=new Row(data.db.getReturnRow());
      for (int i=0; i < data.notfound.size(); i++)       data.notfound.getValue(i).setNull();
    }
    Row newrow=new Row(row);
    newrow.addRow(data.notfound);
    putRow(newrow);
  }
  debug=Messages.getString("DatabaseJoin.Log.CloseQuery");
  data.db.closeQuery(rs);
  debug=Messages.getString("DatabaseJoin.Log.EndOfLookupValues");
}
