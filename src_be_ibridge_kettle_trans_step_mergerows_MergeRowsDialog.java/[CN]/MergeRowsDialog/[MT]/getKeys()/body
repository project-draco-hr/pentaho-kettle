{
  try {
    StepMeta stepMeta=transMeta.findStep(input.getReferenceStepName());
    if (stepMeta != null) {
      Row prev=transMeta.getStepFields(stepMeta);
      if (prev != null) {
        for (int i=0; i < prev.size(); i++) {
          TableItem item=new TableItem(wKeys.table,SWT.NONE);
          item.setText(1,prev.getValue(i).getName());
        }
        wKeys.removeEmptyRows();
        wKeys.setRowNums();
        wKeys.optWidth(true);
      }
    }
  }
 catch (  KettleException e) {
    new ErrorDialog(shell,Messages.getString("MergeRowsDialog.ErrorGettingFields.DialogTitle"),Messages.getString("MergeRowsDialog.ErrorGettingFields.DialogMessage"),e);
  }
}
