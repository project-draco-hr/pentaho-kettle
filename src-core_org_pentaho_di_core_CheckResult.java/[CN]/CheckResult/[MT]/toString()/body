{
  StringBuffer sb=new StringBuffer();
  sb.append(typeDesc[type]).append(": ").append(text);
  if (sourceMeta != null)   sb.append(" (").append(sourceMeta.getName()).append(")");
  return sb.toString();
}
