{
  if (txt.indexOf("?") >= 0) {
    txt=txt.replace((CharSequence)"?+","+");
    txt=txt.replace((CharSequence)"?:",":");
    txt=txt.replace((CharSequence)"?'","'");
    txt=txt.replace((CharSequence)"??","?");
  }
  return StringEscapeUtils.escapeXml(txt);
}
