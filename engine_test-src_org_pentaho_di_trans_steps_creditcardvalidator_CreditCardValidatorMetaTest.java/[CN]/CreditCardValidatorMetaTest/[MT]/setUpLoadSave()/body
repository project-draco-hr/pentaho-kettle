{
  KettleEnvironment.init();
  PluginRegistry.init(true);
  ArrayList<String> attributes=new ArrayList<String>(){
{
      add("dynamicField");
      add("cardType");
      add("notValidMsg");
      add("resultFieldName");
      add("onlyDigits");
    }
  }
;
  loadSaveTester=new LoadSaveTester(testMetaClass,attributes,new HashMap<String,String>(),new HashMap<String,String>(),new HashMap<String,FieldLoadSaveValidator<?>>(),new HashMap<String,FieldLoadSaveValidator<?>>());
}
