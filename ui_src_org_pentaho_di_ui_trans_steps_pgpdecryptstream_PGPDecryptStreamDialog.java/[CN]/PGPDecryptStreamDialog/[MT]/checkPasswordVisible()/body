{
  String password=wPassphrase.getText();
  List<String> list=new ArrayList<String>();
  StringUtil.getUsedVariables(password,list,true);
  if (list.size() == 0) {
    wPassphrase.setEchoChar('*');
  }
 else {
    wPassphrase.setEchoChar('\0');
  }
}
