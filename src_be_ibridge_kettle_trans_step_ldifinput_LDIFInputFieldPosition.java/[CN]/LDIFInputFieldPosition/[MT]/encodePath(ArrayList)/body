{
  String encoded="";
  for (int p=0; p < path.size(); p++) {
    LDIFInputFieldPosition pos=(LDIFInputFieldPosition)path.get(p);
    String elementName=pos.toString();
    if (p > 0)     encoded+=LDIFInputField.POSITION_MARKER;
    encoded+=elementName;
  }
  return encoded;
}
