{
  int n=fis.read(bb);
  if (n == -1) {
    return false;
  }
 else {
    bufferSize=endBuffer + n;
    for (int i=0; i < n; i++) {
      byteBuffer[endBuffer + i]=bb[i];
    }
    return true;
  }
}
