{
  boolean future=new Random().nextBoolean();
  Long time=System.currentTimeMillis();
  if (future) {
    time+=new Random().nextInt(time.intValue());
  }
 else {
    time-=new Random().nextInt(time.intValue());
  }
  return new Date(time);
}
