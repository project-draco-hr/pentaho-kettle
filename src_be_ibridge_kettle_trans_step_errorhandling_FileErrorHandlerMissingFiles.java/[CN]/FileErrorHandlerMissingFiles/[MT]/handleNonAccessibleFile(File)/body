{
  handleFile(file);
  try {
    getWriter(DUMMY_SOURCE).write(THIS_FILE_WAS_NOT_ACCESSIBLE);
    getWriter(DUMMY_SOURCE).write(Const.CR);
  }
 catch (  Exception e) {
    throw new KettleException("Could not create NonAccessibleFile for :" + file.getPath(),e);
  }
}
