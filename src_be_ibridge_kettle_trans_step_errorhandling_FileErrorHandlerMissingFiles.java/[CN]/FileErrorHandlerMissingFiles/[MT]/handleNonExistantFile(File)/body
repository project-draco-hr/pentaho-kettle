{
  handleFile(file);
  try {
    getWriter(DUMMY_SOURCE).write(THIS_FILE_DOES_NOT_EXIST);
    getWriter(DUMMY_SOURCE).write(Const.CR);
  }
 catch (  Exception e) {
    throw new KettleException("Could not create NonExistantFile for :" + file.getPath(),e);
  }
}
