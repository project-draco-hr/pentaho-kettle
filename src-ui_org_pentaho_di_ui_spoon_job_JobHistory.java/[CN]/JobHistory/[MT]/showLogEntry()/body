{
  if (rowList == null) {
    wText.setText("");
    return;
  }
  int nr=wFields.table.getSelectionIndex();
  if (nr >= 0 && rowList != null && nr < rowList.size()) {
    RowMetaAndData row=rowList.get(nr);
    String logging=null;
    try {
      logging=row.getString("LOG_FIELD","");
    }
 catch (    KettleValueException e) {
      new ErrorDialog(this.getShell(),Messages.getString("JobHistory.Error.GettingLoggingInfo"),Messages.getString("JobHistory.Error.GettingLogFieldFromLoggingTable"),e);
    }
    if (logging != null) {
      wText.setText(logging);
    }
 else {
      wText.setText("");
    }
  }
}
