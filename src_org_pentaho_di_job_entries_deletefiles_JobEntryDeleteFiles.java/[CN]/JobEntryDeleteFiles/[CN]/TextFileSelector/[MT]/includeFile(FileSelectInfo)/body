{
  boolean rcode=false;
  try {
    String extension=info.getFile().getName().getExtension();
    if (extension.equals(fileExtension) || Const.isEmpty(fileExtension)) {
      if (info.getFile().getType() == FileType.FOLDER) {
        if (deleteFolder && includeSubfolders) {
          rcode=true;
        }
 else {
          rcode=false;
        }
      }
 else {
        rcode=true;
      }
    }
 else {
      rcode=false;
    }
  }
 catch (  Exception e) {
    log.logError(toString(),Messages.getString("JobEntryDeleteFiles.GeneralException",e.getMessage()));
  }
  return rcode;
}
