{
  try {
    Row r=transMeta.getPrevStepFields(stepname);
    if (r != null) {
      TableItemInsertListener listener=new TableItemInsertListener(){
        public boolean tableItemInserted(        TableItem tableItem,        Value v){
          if (v.getType() == Value.VALUE_TYPE_DATE) {
            tableItem.setText(3,Messages.getString("OraBulkLoaderDialog.DateMask.Label"));
          }
 else {
            tableItem.setText(3,"");
          }
          return true;
        }
      }
;
      BaseStepDialog.getFieldsFromPrevious(r,wReturn,1,new int[]{1,2},new int[]{},-1,-1,listener);
    }
  }
 catch (  KettleException ke) {
    new ErrorDialog(shell,Messages.getString("OraBulkLoaderDialog.FailedToGetFields.DialogTitle"),Messages.getString("OraBulkLoaderDialog.FailedToGetFields.DialogMessage"),ke);
  }
}
