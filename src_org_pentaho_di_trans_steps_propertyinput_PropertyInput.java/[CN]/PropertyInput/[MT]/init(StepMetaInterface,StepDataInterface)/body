{
  meta=(PropertyInputMeta)smi;
  data=(PropertyInputData)sdi;
  if (super.init(smi,sdi)) {
    if (!meta.isFileField()) {
    }
    data.rownr=1L;
    data.totalpreviousfields=0;
    return true;
  }
  return false;
}
