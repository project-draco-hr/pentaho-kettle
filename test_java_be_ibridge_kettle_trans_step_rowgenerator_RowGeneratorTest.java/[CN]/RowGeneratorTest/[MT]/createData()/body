{
  List list=new ArrayList();
  Row r=new Row();
  Value values[]={new Value("string","string_value"),new Value("boolean",true),new Value("integer",20L)};
  for (int i=0; i < values.length; i++) {
    r.addValue(values[i]);
  }
  list.add(r);
  list.add(r);
  list.add(r);
  return list;
}
