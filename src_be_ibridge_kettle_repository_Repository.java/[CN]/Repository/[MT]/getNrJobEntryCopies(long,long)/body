{
  int retval=0;
  String sql="SELECT COUNT(*) FROM R_JOBENTRY_COPY WHERE ID_JOB = " + id_job + " AND ID_JOBENTRY = "+ id_jobentry;
  Row r=database.getOneRow(sql);
  if (r != null) {
    retval=(int)r.getValue(0).getInteger();
  }
  return retval;
}
