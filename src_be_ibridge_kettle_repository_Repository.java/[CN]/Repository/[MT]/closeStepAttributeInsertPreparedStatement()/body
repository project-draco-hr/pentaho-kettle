{
  if (psStepAttributesInsert != null) {
    database.insertFinished(psStepAttributesInsert,true);
    database.closePreparedStatement(psStepAttributesInsert);
    psStepAttributesInsert=null;
  }
}
