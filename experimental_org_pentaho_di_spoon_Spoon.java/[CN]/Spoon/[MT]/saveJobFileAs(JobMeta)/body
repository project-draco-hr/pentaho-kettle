{
  boolean saved=false;
  if (rep != null) {
    jobMeta.setID(-1L);
    saved=saveJobRepository(jobMeta,true);
    renameTabs();
  }
 else {
    saved=saveJobXMLFile(jobMeta);
    renameTabs();
  }
  return saved;
}
