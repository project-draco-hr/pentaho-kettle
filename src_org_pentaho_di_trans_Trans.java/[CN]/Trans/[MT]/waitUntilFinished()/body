{
  try {
    while (!finished) {
      Thread.sleep(0,1);
    }
  }
 catch (  Exception e) {
    log.logError(toString(),Messages.getString("Trans.Log.TransformationError") + e.toString());
    log.logError(toString(),Const.getStackTracker(e));
  }
}
