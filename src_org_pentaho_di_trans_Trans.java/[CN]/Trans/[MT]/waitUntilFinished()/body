{
  try {
    while (!finished.get()) {
      Thread.sleep(0,1);
    }
  }
 catch (  Exception e) {
    log.logError(BaseMessages.getString(PKG,"Trans.Log.TransformationError") + e.toString());
    log.logError(Const.getStackTracker(e));
  }
}
