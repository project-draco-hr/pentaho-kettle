{
  try {
    while (!finished.get()) {
      Thread.sleep(0,1);
    }
  }
 catch (  Exception e) {
    log.logError(toString(),BaseMessages.getString(PKG,"Trans.Log.TransformationError") + e.toString());
    log.logError(toString(),Const.getStackTracker(e));
  }
}
