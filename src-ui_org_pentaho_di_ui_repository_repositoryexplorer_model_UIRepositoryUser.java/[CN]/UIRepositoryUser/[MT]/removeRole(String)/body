{
  IRole roleInfo=null;
  for (  IRole role : rui.getRoles()) {
    if (role.getName().equals(roleName)) {
      roleInfo=role;
      break;
    }
  }
  if (roleInfo != null) {
    return rui.removeRole(roleInfo);
  }
 else {
    return false;
  }
}
