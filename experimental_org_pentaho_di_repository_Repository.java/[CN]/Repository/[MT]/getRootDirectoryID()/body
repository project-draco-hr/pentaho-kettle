{
  RowMetaAndData result=database.getOneRow("SELECT ID_DIRECTORY FROM R_DIRECTORY WHERE ID_DIRECTORY_PARENT = 0");
  if (result != null && result.isNumeric(0))   return result.getInteger(0,-1);
  return -1;
}
