{
  String correctsignature=getSignature();
  if (licenseCode != null && licenseCode.length() > 0 && licenseCode.equalsIgnoreCase(correctsignature) && (products.toUpperCase().indexOf(product.toUpperCase()) >= 0 || products.equalsIgnoreCase(Licenses.PRODUCT_ALL)))   return true;
  return false;
}
