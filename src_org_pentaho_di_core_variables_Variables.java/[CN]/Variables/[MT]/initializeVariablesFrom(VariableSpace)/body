{
  this.parent=parent;
  properties.putAll(System.getProperties());
  if (parent != null) {
    copyVariablesFrom(parent);
  }
  if (injection != null) {
    properties.putAll(injection);
    injection=null;
  }
  initialized=true;
}
