{
  if (repositoryElement.getID() < 0) {
    try {
      if (getTransformationID(repositoryElement.getName(),repositoryElement.getRepositoryDirectory().getID()) > 0)       return true;
    }
 catch (    KettleException dbe) {
      return true;
    }
  }
  return false;
}
