{
  try {
    URL xulFile=getAndValidate(resource);
    Document doc=XMLHandler.loadXMLFile(xulFile);
    return MenuHelper.createToolbarFromXul(doc,shell,xulMessages,caller);
  }
 catch (  IOException e) {
    throw new KettleException(e);
  }
}
