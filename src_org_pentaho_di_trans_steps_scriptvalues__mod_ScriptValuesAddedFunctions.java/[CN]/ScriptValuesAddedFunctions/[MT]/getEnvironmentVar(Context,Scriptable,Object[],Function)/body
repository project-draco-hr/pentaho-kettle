{
  String sRC="";
  if (ArgList.length == 1) {
    try {
      String sArg1=Context.toString(ArgList[0]);
      sRC=variables.getVariable(sArg1,"");
    }
 catch (    Exception e) {
      sRC="";
    }
  }
  return sRC;
}
