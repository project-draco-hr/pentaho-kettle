{
  if (input != null) {
    try {
      data.readerRowSet=data.reader.parse(input);
      return true;
    }
 catch (    KettleException ke) {
      logInputError(ke);
    }
catch (    Exception e) {
      logInputError(e);
    }
  }
  return false;
}
