{
  if (!gotPreviousFields) {
    gotPreviousFields=true;
    try {
      String sqlfield=wSQLFieldName.getText();
      wSQLFieldName.removeAll();
      RowMetaInterface r=transMeta.getPrevStepFields(stepname);
      if (r != null) {
        wSQLFieldName.removeAll();
        wSQLFieldName.setItems(r.getFieldNames());
      }
      if (sqlfield != null)       wSQLFieldName.setText(sqlfield);
    }
 catch (    KettleException ke) {
      new ErrorDialog(shell,Messages.getString("DynamicSQLRowDialog.FailedToGetFields.DialogTitle"),Messages.getString("DynamicSQLRowDialog.FailedToGetFields.DialogMessage"),ke);
    }
  }
}
