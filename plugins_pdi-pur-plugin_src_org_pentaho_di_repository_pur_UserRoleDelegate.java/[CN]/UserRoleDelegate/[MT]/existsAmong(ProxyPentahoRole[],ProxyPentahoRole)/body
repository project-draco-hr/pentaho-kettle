{
  if (existing != null) {
    String name=role.getName();
    for (    ProxyPentahoRole pentahoRole : existing) {
      if (name.equals(pentahoRole.getName())) {
        return true;
      }
    }
  }
  return false;
}
