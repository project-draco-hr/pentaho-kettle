{
  StringBuffer retval=new StringBuffer();
  if (formula != null)   for (int i=0; i < formula.length; i++) {
    retval.append("       " + formula[i].getXML() + Const.CR);
  }
  return retval.toString();
}
