{
  CTabItem tabItem=spoon.findCTabItem(slaveServer.getServerAndPort());
  if (tabItem != null) {
    timerTask.cancel();
    timer.cancel();
    spoon.tabfolder.setSelection(0);
    tabItem.dispose();
  }
}
