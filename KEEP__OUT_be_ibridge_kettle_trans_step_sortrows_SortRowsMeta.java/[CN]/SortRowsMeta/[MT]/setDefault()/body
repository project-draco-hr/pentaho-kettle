{
  directory="%%java.io.tmpdir%%";
  prefix="out";
  sortSize=Const.SORT_SIZE;
  compressFiles=true;
  int nrfields=0;
  allocate(nrfields);
  for (int i=0; i < nrfields; i++) {
    fieldName[i]="field" + i;
  }
}
