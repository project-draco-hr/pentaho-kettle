{
  boolean retval=false;
  try {
    data.workbook.write();
    data.workbook.close();
    data.formats.clear();
    retval=true;
  }
 catch (  Exception e) {
    logError("Unable to close openFile file : " + e.toString());
  }
  return retval;
}
