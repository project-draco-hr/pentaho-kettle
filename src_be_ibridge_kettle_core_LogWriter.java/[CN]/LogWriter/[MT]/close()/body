{
  boolean retval=true;
  try {
    logs.remove(getFilename());
    if (fileAppender != null) {
      fileAppender.close();
      rootLogger.removeAppender(fileAppender);
      fileAppender=null;
    }
  }
 catch (  Exception e) {
    retval=false;
  }
  return retval;
}
