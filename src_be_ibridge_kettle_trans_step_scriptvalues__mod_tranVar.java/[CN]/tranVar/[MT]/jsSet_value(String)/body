{
  try {
    Value vx=new Value("x");
    vx.setType(Value.VALUE_TYPE_STRING);
    vx.setValue(value);
    this.tValue=vx;
  }
 catch (  Exception e) {
    System.out.println("Error setValue" + e.toString());
  }
}
