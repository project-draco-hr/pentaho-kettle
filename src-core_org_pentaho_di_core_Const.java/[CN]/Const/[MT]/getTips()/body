{
  if (tips == null) {
    int tipsOfDayCount=toInt(Messages.getString("Const.TipOfDay.Count"),0);
    tips=new String[tipsOfDayCount];
    for (int i=1; i <= tipsOfDayCount; i++)     tips[i - 1]=Messages.getString("Const.TipOfDay" + Integer.toString(i));
  }
  return tips;
}
