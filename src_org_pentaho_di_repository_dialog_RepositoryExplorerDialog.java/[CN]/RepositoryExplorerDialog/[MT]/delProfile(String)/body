{
  try {
    long idProfile=rep.getProfileID(profilename);
    if (idProfile > 0) {
      rep.delProfile(idProfile);
    }
    refreshTree();
  }
 catch (  KettleException e) {
    new ErrorDialog(shell,Messages.getString("RepositoryExplorerDialog.Profile.Delete.UnexpectedError.Title"),Messages.getString("RepositoryExplorerDialog.Profile.Delete.UnexpectedError.Message"),e);
  }
}
