{
  String retval="";
  if (hasAggregate() && !isExact()) {
    retval+="F___" + fieldnr;
  }
 else   if (isExact()) {
    retval+="E___" + fieldnr;
  }
 else {
    retval+=getDBName();
  }
  return retval;
}
