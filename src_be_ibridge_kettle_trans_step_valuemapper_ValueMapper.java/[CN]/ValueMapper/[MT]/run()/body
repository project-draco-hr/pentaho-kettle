{
  try {
    logBasic(Messages.getString("ValueMapper.Log.StartingToRun"));
    while (processRow(meta,data) && !isStopped())     ;
  }
 catch (  Exception e) {
    logError(Messages.getString("ValueMapper.RuntimeError.UnexpectedError.VALUEMAPPER0003",debug,e.toString()));
    logError(Const.getStackTracker(e));
    setErrors(1);
    stopAll();
  }
 finally {
    dispose(meta,data);
    logSummary();
    markStop();
  }
}
