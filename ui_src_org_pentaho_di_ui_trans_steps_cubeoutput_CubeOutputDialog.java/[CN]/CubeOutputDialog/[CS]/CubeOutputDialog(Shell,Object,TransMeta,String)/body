{
  super(parent,(BaseStepMeta)in,transMeta,sname);
  input=(CubeOutputMeta)in;
  this.transMeta=transMeta;
  if (sname != null)   stepname=sname;
 else   stepname=BaseMessages.getString(PKG,"CubeOutputDialog.DefaultStepName");
}
