{
  try {
    logBasic(Messages.getString("MergeRows.Log.StartingToRun"));
    while (processRow(meta,data) && !isStopped())     ;
  }
 catch (  Exception e) {
    logError(Messages.getString("MergeRows.Log.UnexpectedError") + debug + "' : "+ e.toString());
    setErrors(1);
    stopAll();
  }
 finally {
    dispose(meta,data);
    logSummary();
    markStop();
  }
}
