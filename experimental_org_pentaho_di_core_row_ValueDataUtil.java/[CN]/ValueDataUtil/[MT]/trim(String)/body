{
  int max=string.length() - 1;
  int min=0;
  while (min <= max && isSpace(string.charAt(min)))   min++;
  while (max >= 0 && isSpace(string.charAt(max)))   max--;
  if (max < min)   return "";
  return string.substring(min,max + 1);
}
