{
  try {
    ColInfo[] convertedVals=KettleHelper.convert(null,singleColumnString);
    assertNull(convertedVals);
  }
 catch (  Exception ex) {
    fail("Got exception when testing Null row meta conversion" + ex.getMessage());
  }
}
