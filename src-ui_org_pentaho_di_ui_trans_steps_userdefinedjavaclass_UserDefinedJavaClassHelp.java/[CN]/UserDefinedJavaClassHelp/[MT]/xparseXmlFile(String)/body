{
  try {
    String fileContents=parseFile(strFileName);
    dom=XMLHandler.loadXMLString(fileContents);
  }
 catch (  Exception e) {
    throw new KettleXMLException("Unable to read script values help file from file [" + strFileName + "]",e);
  }
}
