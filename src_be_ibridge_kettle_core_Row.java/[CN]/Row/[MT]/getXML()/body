{
  StringBuffer xml=new StringBuffer();
  xml.append("<" + XML_TAG + ">");
  for (int i=0; i < size(); i++) {
    xml.append(getValue(i).getXML());
  }
  xml.append("</" + XML_TAG + ">");
  return xml.toString();
}
