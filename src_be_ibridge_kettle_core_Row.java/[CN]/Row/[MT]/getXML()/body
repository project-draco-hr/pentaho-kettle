{
  String retval="<row>";
  retval+=XMLHandler.addTagValue("logdate",logdate);
  for (int i=0; i < size(); i++) {
    retval+=getValue(i).getXML();
  }
  retval+="</row>" + Const.CR;
  return retval;
}
