{
  String retval="[";
  for (int i=0; i < size(); i++) {
    Value value=getValue(i);
    if (i != 0)     retval+=", ";
    if (value != null) {
      retval+=value.getName() + "=" + value.toString(false);
    }
 else {
      retval+="NULL";
    }
  }
  retval+="]";
  return retval;
}
