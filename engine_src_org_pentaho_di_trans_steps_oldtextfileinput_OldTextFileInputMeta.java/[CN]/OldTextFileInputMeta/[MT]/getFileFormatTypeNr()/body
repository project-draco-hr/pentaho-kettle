{
  if (getFileFormat().equalsIgnoreCase("DOS")) {
    return FILE_FORMAT_DOS;
  }
 else   if (getFileFormat().equalsIgnoreCase("unix")) {
    return OldTextFileInputMeta.FILE_FORMAT_UNIX;
  }
 else {
    return OldTextFileInputMeta.FILE_FORMAT_MIXED;
  }
}
