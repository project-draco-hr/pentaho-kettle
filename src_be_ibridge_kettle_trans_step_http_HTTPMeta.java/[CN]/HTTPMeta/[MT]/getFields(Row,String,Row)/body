{
  Row row;
  if (r == null)   row=new Row();
 else   row=r;
  if (!Const.isEmpty(fieldName)) {
    Value v=new Value(fieldName,Value.VALUE_TYPE_STRING);
    v.setOrigin(name);
    row.addValue(v);
  }
  return row;
}
