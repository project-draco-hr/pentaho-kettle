{
  if (getRepositoryDirectory().isRoot())   return getRepositoryDirectory().getPath() + getName();
 else   return getRepositoryDirectory().getPath() + RepositoryDirectory.DIRECTORY_SEPARATOR + getName();
}
