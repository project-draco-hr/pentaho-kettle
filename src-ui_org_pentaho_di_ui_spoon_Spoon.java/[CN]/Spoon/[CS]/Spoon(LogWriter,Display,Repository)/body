{
  this.log=log;
  this.rep=rep;
  if (d != null) {
    display=d;
    destroy=false;
  }
 else {
    display=new Display();
    destroy=true;
  }
  shell=new Shell(display);
  shell.setText(APPL_TITLE);
  staticSpoon=this;
  SpoonFactory.setSpoonInstance(this);
}
