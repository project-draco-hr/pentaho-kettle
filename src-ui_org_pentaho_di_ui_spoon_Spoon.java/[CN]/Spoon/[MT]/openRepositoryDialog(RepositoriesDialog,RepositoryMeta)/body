{
  if (rd.open()) {
    Repository repository=rd.getConnectedRepository();
    setRepository(repository);
    return true;
  }
 else {
    if (rd.isCancelled()) {
      return false;
    }
  }
  return true;
}
