{
  TransMeta transMeta=getActiveTransformation();
  if (transMeta != null) {
    delegates.trans.closeTransformation(transMeta);
  }
 else {
    JobMeta jobMeta=getActiveJob();
    if (jobMeta != null)     delegates.jobs.closeJob(jobMeta);
  }
}
