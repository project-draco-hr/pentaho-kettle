{
  boolean closed=true;
  TransMeta transMeta=getActiveTransformation();
  if (transMeta != null) {
    closed=tabCloseSelected();
  }
 else {
    JobMeta jobMeta=getActiveJob();
    if (jobMeta != null)     closed=tabCloseSelected();
  }
  return closed;
}
