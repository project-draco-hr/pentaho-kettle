{
  int size=getSize();
  if (size > 0) {
    setErrorMessage(null);
    return true;
  }
 else {
    setErrorMessage(Messages.getString("TextFileImportWizardPage1.ErrorMarkerNeeded"));
    return false;
  }
}
