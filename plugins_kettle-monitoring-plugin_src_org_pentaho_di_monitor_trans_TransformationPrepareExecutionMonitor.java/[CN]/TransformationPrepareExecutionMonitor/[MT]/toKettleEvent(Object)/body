{
  if (o == null || !(o instanceof Trans)) {
    return null;
  }
  TransformationEvent event=new TransformationEvent(TransformationEvent.EventType.BEGIN_PREPARE_EXECUTION).build((Trans)o);
  logInfo("[PDI Extension Point Plugin] Dispathing to Event Bus " + event.toString());
  return event;
}
