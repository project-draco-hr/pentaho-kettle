{
  if (o == null || !(o instanceof Trans)) {
    return null;
  }
  TransformationEvent event=new TransformationEvent(EventType.Transformation.BEGIN_PREPARE_EXECUTION).build((Trans)o);
  return event;
}
