{
  setStopped(true);
  cursor_hand.dispose();
  cursor_hourglass.dispose();
  if (destroy && !display.isDisposed()) {
    try {
      display.dispose();
    }
 catch (    SWTException e) {
    }
  }
}
