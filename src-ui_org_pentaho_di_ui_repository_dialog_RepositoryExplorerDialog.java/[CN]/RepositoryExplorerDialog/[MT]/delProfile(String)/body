{
  try {
    ObjectId idProfile=rep.getProfileID(profilename);
    if (idProfile != null) {
      rep.delProfile(idProfile);
    }
    refreshTree();
  }
 catch (  KettleException e) {
    new ErrorDialog(shell,BaseMessages.getString(PKG,"RepositoryExplorerDialog.Profile.Delete.UnexpectedError.Title"),BaseMessages.getString(PKG,"RepositoryExplorerDialog.Profile.Delete.UnexpectedError.Message"),e);
  }
}
