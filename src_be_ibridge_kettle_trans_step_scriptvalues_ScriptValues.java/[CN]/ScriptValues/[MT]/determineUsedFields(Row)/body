{
  int nr=0;
  for (int i=0; i < row.size(); i++) {
    String valname=row.getValue(i).getName().toUpperCase();
    if (meta.getScript().toUpperCase().indexOf(valname) >= 0) {
      nr++;
    }
  }
  data.fields_used=new int[nr];
  nr=0;
  for (int i=0; i < row.size(); i++) {
    String valname=row.getValue(i).getName();
    if (meta.getScript().indexOf(valname) >= 0) {
      if (log.isDetailed())       logDetailed(Messages.getString("ScriptValues.Log.UsedValueName",String.valueOf(i),valname));
      data.fields_used[nr]=i;
      nr++;
    }
  }
  if (log.isDetailed())   logDetailed(Messages.getString("ScriptValues.Log.UsingValuesFromInputStream",String.valueOf(data.fields_used.length)));
}
