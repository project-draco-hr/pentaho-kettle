{
  FileObject dest=VFS.getManager().resolveFile(Const.getKettleDirectory());
  dest.createFolder();
  FileObject destFile=dest.resolveFile(parent.getName().getBaseName());
  destFile.createFolder();
  destFile.copyFrom(!(parent instanceof JarFileObject) ? VFS.getManager().resolveFile("jar:" + parent.getName().getURI()) : parent,new AllFileSelector());
  return destFile;
}
