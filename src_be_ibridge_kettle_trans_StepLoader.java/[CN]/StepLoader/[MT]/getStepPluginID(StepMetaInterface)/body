{
  for (int i=0; i < nrStepsWithType(StepPlugin.TYPE_ALL); i++) {
    StepPlugin sp=getStepWithType(StepPlugin.TYPE_ALL,i);
    if (sp.getClassname() == sii.getClass().getName())     return sp.getID()[0];
  }
  return null;
}
