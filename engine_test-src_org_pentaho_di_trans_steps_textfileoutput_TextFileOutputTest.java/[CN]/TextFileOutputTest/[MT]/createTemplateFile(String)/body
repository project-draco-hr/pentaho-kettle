{
  File f2=createTemplateFile();
  if (content == null) {
    f2.delete();
  }
 else {
    FileWriter fw=null;
    try {
      fw=new FileWriter(f2);
      fw.write(content);
    }
  finally {
      if (fw != null) {
        fw.close();
      }
    }
  }
  return f2;
}
