{
  if (tt == null)   return 0;
  for (int i=0; i < successBooleanConditionDesc.length; i++) {
    if (successBooleanConditionDesc[i].equalsIgnoreCase(tt))     return i;
  }
  return getSuccessBooleanByCode(tt);
}
