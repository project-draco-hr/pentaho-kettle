{
  if (tt == null)   return 0;
  for (int i=0; i < successNumberConditionDesc.length; i++) {
    if (successNumberConditionDesc[i].equalsIgnoreCase(tt))     return i;
  }
  return getSuccessNumberByCode(tt);
}
