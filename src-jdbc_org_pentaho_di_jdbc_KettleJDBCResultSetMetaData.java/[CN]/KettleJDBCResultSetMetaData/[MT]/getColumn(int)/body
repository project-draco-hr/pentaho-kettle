{
  if (column < 1 || column > columnCount) {
    throw new SQLException(BaseMessages.getString(PKG,"error.resultset.colindex",Integer.toString(column)),"07009");
  }
  log.debug("getColumn");
  return columns[column - 1];
}
