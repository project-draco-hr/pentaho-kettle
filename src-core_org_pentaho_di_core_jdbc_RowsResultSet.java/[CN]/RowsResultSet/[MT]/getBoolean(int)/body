{
  try {
    Boolean b=rowMeta.getBoolean(rows.get(currentIndex),index - 1);
    if (b == null) {
      lastNull=true;
    }
    lastNull=false;
    return b;
  }
 catch (  Exception e) {
    throw new SQLException(e);
  }
}
