{
  try {
    byte[] binary=rowMeta.getBinary(rows.get(currentIndex),index - 1);
    if (binary == null) {
      lastNull=true;
      return null;
    }
    lastNull=false;
    return binary;
  }
 catch (  Exception e) {
    throw new SQLException(e);
  }
}
