{
  try {
    String string=rowMeta.getString(rows.get(currentIndex),index - 1);
    if (string == null) {
      lastNull=true;
    }
    lastNull=false;
    return string;
  }
 catch (  Exception e) {
    throw new SQLException(e);
  }
}
