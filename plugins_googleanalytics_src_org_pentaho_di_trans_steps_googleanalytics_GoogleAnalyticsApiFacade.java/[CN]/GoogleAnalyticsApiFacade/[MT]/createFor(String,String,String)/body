{
  URI keyFileURI=new URI(oauthKeyFile);
  File keyFile=keyFileURI.isAbsolute() ? new File(keyFileURI) : new File(oauthKeyFile);
  return new GoogleAnalyticsApiFacade(GoogleNetHttpTransport.newTrustedTransport(),JacksonFactory.getDefaultInstance(),application,oauthServiceAccount,keyFile);
}
